# Environment reference — copy to .env or set via fly secrets (placeholders only).

PORT=3000
# DASHBOARD_TOKEN=

APIFY_API_TOKEN=YOUR_APIFY_TOKEN
TIKTOK_USERNAME=puga.realtor

# DB_JSON_PATH=data/local-dashboard-db.json
AUTO_PLANS_JSON_PATH=data/auto-plans.json

ANTHROPIC_API_KEY=YOUR_ANTHROPIC_KEY
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
# HARVEY_MODEL=claude-sonnet-4-6

# Content Manager Brain + OpenShorts clip analysis (Anthropic Claude — uses ANTHROPIC_API_KEY above)
# CONTENT_MODEL_FAST=claude-haiku-4-5-20251001
# CONTENT_MODEL_QUALITY=claude-sonnet-5
# CONTENT_BRAIN_MODEL=claude-sonnet-5

# GEMINI_API_KEY below is Harvey-only now (voice TTS + Gemini Live) — the
# Content Manager and OpenShorts sidecar no longer use Gemini for anything.
GEMINI_API_KEY=YOUR_GEMINI_KEY
OPENSHORTS_URL=http://localhost:8000
OPENSHORTS_PORT=8000
# Optional clip enhancements (all default on except B-roll, which needs a key)
# OPENSHORTS_AUTO_ZOOM=true
# OPENSHORTS_AUTO_ZOOM_AMOUNT=0.05
# PEXELS_API_KEY=          # free key from pexels.com/api — enables B-roll overlay
# OPENSHORTS_BROLL=true
# VoxCPM2 GPU microservice (RunPod / Modal / local GPU — NOT Fly.io)
VOXCPM_API_URL=
GEMINI_LIVE_MODEL=models/gemini-2.0-flash-live-001

AD_DASHBOARD_BASE_URL=https://marco-agents.fly.dev

BRIVITY_API_KEY=YOUR_BRIVITY_KEY
BRIVITY_BASE_URL=https://app.brivityidx.com/api

# Gmail (Harvey gmail_send tool + module 07) — OAuth account: pmarcopuga@gmail.com
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
# GMAIL_FROM=optional — defaults to OAuth account profile email
MARCO_EMAIL=pmarcopuga@gmail.com
# MARCO_EMAIL_ADDRESS=pmarcopuga@gmail.com

# Harvey WhatsApp owner (must match Marco's WhatsApp for owner tools on OpenClaw)
# HARVEY_OWNER_NUMBER=+19804430453

# Twilio SMS
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
TWILIO_WEBHOOK_AUTH_TOKEN=

# Finance — pace ring & net commission split (set real values in .env)
# MONTHLY_GCI_GOAL=50000
# BROKERAGE_SPLIT_PCT=0.5
# MARCO_PHONE_NUMBER=+1...

# Analytics — Zernio social analytics provider (section 5).
# Unset = Instagram/Facebook/YouTube tabs report "not connected" and show no
# numbers at all. Nothing is fabricated when this is missing.
# ZERNIO_API_KEY=
# ZERNIO_BASE_URL=https://api.zernio.com

# Browser control — pairing token for the Harvey Chrome extension.
# Unset = browser control is disabled entirely (fails closed).
# BROWSER_CONTROL_TOKEN=

# Browser control — which paired machine wins an unaddressed command.
# Comma-separated, case-insensitive, matched as a substring of the device name
# set in the extension popup. Default "marco" — it is his system.
# BROWSER_DEVICE_PRIORITY=marco,wesley
