# FORAI — Marcos-90-automations (marco-90-automation)

Last updated: 2026-08-03 by Claude Code

FORAI = "For AI." This is the living architectural summary of this repo — the source of truth agents read before working here, and the source the AETHON Chronicler pulls nightly to keep the master architecture docs current. Keep it short: current state, recent changes, known gaps. Not a commit log.

**Convention (non-negotiable, same as the green-build rule):** whenever you finish meaningful architectural work here — a new subsystem, endpoint, table, integration, or a change to how something works — add a dated line to `## Recent changes` (most recent first) and refresh Architecture / Known gaps if they changed. Not every commit — anything that changes what someone reading the architecture needs to know. Flag anything security/breaking explicitly (the words "security", "unauthenticated", "breaking change" route it to the Security register and ping Telegram).

## What this system is

A single-tenant business automation platform built for one real-estate agent (Marco Puga). It runs the full funnel: Instagram/ManyChat DMs and SMS in, an AI qualification pipeline, a Brivity-backed CRM, transaction and finance tracking, email marketing, lead-nurture and reporting agents, plus a short-form content pipeline that turns long video into published vertical clips. On top of it sits "Harvey," an operator-facing AI assistant with persistent memory that can read and act on the whole business state.

It deploys as one Docker image to Fly.io (app `marco-90-automation`, region `dfw`, 4 GB `performance-2x`, `/data` volume). Supervisord runs three processes in that image: the Node server on :3000, the OpenShorts Python video sidecar on :8000, and a CapCutAPI draft generator on :9001. Pushes to `main` (or `master`) trigger GitHub Actions, which builds with a buildx layer cache, pushes to `registry.fly.io`, and runs `flyctl deploy --image`.

## Architecture

- **`src/server.ts`** — one monolithic Express app (~10.5k lines, ~395 routes registered directly on `app`, no Routers). Also hosts the WebSocket upgrade router (ElevenLabs STT → Deepgram fallback → Hull `/ws`) and an OpenAI-compatible `/v1/chat` surface used by the WhatsApp gateway.
- **`src/core/`** — persistence + domain layer. ~12 `better-sqlite3` stores, each owning its own file under `/data` (`content.db`, `social.db`, `transactions.db`, `finance.db`, `email.db`, `auth.db`, `ads.db`, `sms.db`, `leadscores.db`, `reporting.db`, `crm-automation.db`, `voice-clone.db`) plus ~11 JSON stores (`local-dashboard-db.json` is the main app DB: leads, conversations, tasks, dashboard snapshot). `types.ts` holds shared types and the role/permission model.
- **`src/agents/`** — 22 scheduled/autonomous agent families: `contentManager` (largest — ingest → clip → enhance → publish, with a Claude-driven `brain/` strategy loop), `emailMarketing`, `finance`, `reporting`, `leadNurture`, `transactionFlows`, `leadScoring`, `socialMedia`, `voiceClone`, escalations, reminders, and re-engagement.
- **`src/harvey/`** — the operator AI: `perception` (fresh business snapshot) → `judgment` (focus areas/directives) → `communication` (Sonnet operator voice), with Anthropic tool definitions in `tools.ts` and persistent episodic/semantic memory in `harvey-memory.db`.
- **`src/hull/`** — the general agent runtime under Harvey ("Aethon hull"): agent loop, model routing, graph memory (`aethon-memory.db`), voice STT/TTS, WebSocket transport.
- **`src/app/` + `src/modules/`** — the DM request path: webhook intake → funnel state → numbered pipeline steps (01–12) → Claude reply.
- **`src/integrations/`** — thin per-vendor clients: Brivity, Twilio, Sinch, Apify (TikTok), Gmail/Google Drive, ManyChat, upload-post, ElevenLabs, Anthropic, OpenShorts, Forewarn.
- **`public/`** — ~20 standalone HTML pages, no build step and no framework. `shell.html` is the app shell that iframes the rest; `crm-brivity.html` is the Brivity-replica CRM (the centerpiece), alongside `jarvis.html` (Harvey), `social.html` (Content Manager), `dashboard.html`, `ads.html`, `finance.html`, `reporting.html`, and others.
- **`services/openshorts/`** — Python/FastAPI video sidecar. Upstream `mutonby/openshorts` cloned at build, with Marco-specific `*_marco.py` modules layered on: transcription, LLM viral-moment detection, gaze/take analysis, auto-reframe, burned ASS captions, emoji overlays, dead-air removal. Concurrency- and memory-guarded for the 4 GB VM.
- **`vendor/openreel`** — vendored MIT browser clip editor served same-origin at `/editor`, patched to load a clip by `?clip=<id>&token=`.
- **Auth (built, not enforced)** — session cookie `mp_sid` + scrypt passwords + `sessions`/`login_history`/`audit_log` in `auth.db`, gated behind the `SITE_LOGIN_ENABLED` env kill switch. Legacy API auth is `DASHBOARD_TOKEN`. See Known gaps.

## Recent changes (most recent first)

- [2026-08-03] — **The criteria parser was storing sentence fragments as cities; fixed against the MLS itself, and MLS is now a tab inside the CRM** (`src/core/criteriaExtract.ts` (new), `src/core/listingsStore.ts`, `src/app/funnelDeterministic.ts`, `src/modules/06-criteria-pivot-email/index.ts`, `src/server.ts`, `public/crm-brivity.html`, `public/listings.html`). **THE BUG: `extractArea` was "the words after `in`", up to four of them.** It ran on every inbound DM, so "I'm in the market for something" recorded a target area of **"the market for something"**. Of **762 production leads, 36 had an area and only 4 were a place**: the rest were `"my own my home"`, `"the mornings"`, `"a home and this"`, `"the property if it"`, `"la cantera how far"`, `"just a moment"`. This is why the MLS wiring looked empty — every guard downstream was correctly refusing to treat "the mornings" as a city. **THE FIX CHECKS AGAINST REALITY:** new `knownCities()` reads the distinct cities out of the 27,083-listing mirror (280 of them, cached hourly, ≥2 listings each), so the feed itself is the authority on whether a phrase is a place — no hand-kept town list to drift from what Marco can actually sell. Two tiers: a known MLS city anywhere in the message at any casing returns the **canonical** name ("looking around boerne" → `Boerne`, which `listings.city` matches exactly); anything else must read as a **proper noun in the lead's own typing** to be kept at all, which preserves out-of-area signals like `Austin` and `Grand Rapids` while rejecting "the mornings". Longest-name-first matching stops "New Braunfels" being shadowed, and matching is word-bounded so "converseation" is not Converse. Neighbourhood words (`downtown`, `north side`) are rejected — they match no city — but a **bare** direction is separated from a directional name, so "the north side" is dropped while `West Lake Hills` and `North Richland Hills` survive. `normalizeArea()` also repairs stored values, and runs on the CRM's PATCH so a hand-typed "san antonio" is saved as "San Antonio"; `funnelDeterministic` and module 06 normalise the stored area before trusting it, so a lead self-heals the next time they message. **THE SAME BUG IN THE PRICE PARSER:** `extractPriceCap` was "a number of 3+ digits is dollars", so a lead who typed their phone number had a budget of **$9,566,489,575** — **11 of 13 stored price caps on production were phone numbers**, and since the shortlist searches from 60% of the cap upwards, those leads matched nothing while looking fully qualified. Now: a run of 10+ digits is a phone number and is skipped, a ceiling of $25M applies (the dearest home in the whole mirror is $10.3M), and **"400k" / "1.2m" are finally understood** — the old rule read "under 400k" as the number 400 and stored no budget at all. `isJunkPriceCap` clears bad stored caps on read and on the CRM PATCH. The module is now `src/core/criteriaExtract.ts` since it covers more than area. **DATA REPAIR:** 5 rows held a real city inside debris and were normalised (`"San Antonio yes"` → `San Antonio`, `"the new braunfels and"` → `New Braunfels`, `"the RGV area so"` → `RGV`, `"Fortworth Dallas"` → `Dallas`, `"the Boerne area so"` → `Boerne`); 27 held no place at all and were cleared, and 11 phone-number price caps were cleared. Nothing is lost — the original wording is still in each lead's conversation. **MLS INSIDE THE CRM:** new rail tab that **embeds `/listings` itself** rather than reimplementing it, so the two cannot drift into showing different prices for the same house. Wired both ways: a lead's panel has "Search MLS for this lead", which opens the tab filtered to their city/price/beds and names them in a banner; each card then offers "set as their property", which PATCHes `mlsListingKey` (now accepted by `/api/crm/lead/:id`) and posts back so the lead's panel updates without a reload. Clicking the linked one again unlinks it — a wrong link is worse than none, since everything downstream quotes it as "the home you asked about". Verified **79/79** on the parser (every rejection case is a real production value, area and price alike) and **23/23 in a real browser** driving the whole loop: the tab embeds the live page showing the same 27,083 count, a lead's criteria reach the filters, linking writes through to the lead record at the same price the tab displayed, the linked home is not also offered as an alternative to itself, and unlinking clears it.

- [2026-08-02] — **CRM and the email drips now read the live MLS instead of describing property from a field somebody typed weeks ago** (`src/core/listingsStore.ts`, `src/core/listingMatch.ts`, `src/agents/emailMarketing/{buyerDrip,sellerDrip,pastClientQuarterly,noReplyFollowup}.ts`, `src/harvey/platformTools.ts`, `src/server.ts`, `public/crm-brivity.html`, `public/dashboard.html`, `src/core/types.ts`, `src/core/db.ts`). **The constraint that shaped all of it: this SABOR feed carries Active and Pending only — 2 of 27,083 rows have a close price — so it has NO SOLD DATA.** Every seller-facing figure is therefore an asking price and is labelled as one in the copy itself ("Those are asking prices, not sale prices"), because a seller who prices against a list price mistaken for a sale price sits on the market for months. New `marketStats(city)` returns active/pending counts, a true median asking price (SQL median via OFFSET, not an average — one $12M ranch would otherwise set a suburb's number), median size, and new-in-30-days; it returns **null below 5 active listings and null for free text** like "north side", so a lead in Boerne can never be quoted San Antonio numbers. **New `Lead.mlsListingKey`** — written only on an exact match, kept separate from `propertyInquired` (the free text is what they said, this is what we resolved it to), and persisted by the DM pipeline so a lead's property cannot silently change identity as the conversation grows. **Drips:** buyer steps 3 and 5 send real active listings and the live state of their own property; seller step 3 replaced "real numbers, not guesses" (which contained no numbers) with the inventory they are competing against; seller steps 1/4/5, the past-client Q2 "market update" and the day-7 no-reply follow-up ("something that might help", which previously contained nothing at all) likewise. **Every one falls back to its original copy when there is nothing genuine** — a shortlist email with no homes, or homes picked because we had no criteria, is worse than the generic version it replaced. **CRM:** new `GET /api/leads/:id/mls` (dashboard-token gated, raw SimplyRETS record stripped) feeding a live panel in the lead detail rail of **both** `/crm` (`crm-brivity.html`, the real CRM) and `dashboard.html`'s CRM-lite — their property read fresh with a **Pending badge styled as a warning**, other homes matching their criteria, and city inventory with the no-sold-data caveat printed. It renders **nothing at all** when there is nothing real, so it collapses rather than showing an empty heading. **Harvey's `get_lead` gained an `mls` block** with the same three parts plus an explicit note that `matchesTheirCriteria` must never be described as the home they asked about. **Two bugs the tests caught:** the shortlist was three consecutive doors on one road (the mirror is ordered by recency and new-build subdivisions dominate it) — now one home per street, then fill; and a lead's own home was being offered back as an "alternative" to itself, now excluded at source in `listingsForCriteria`. Verified **62/62** against the real 27,083-listing mirror (drip copy, fallbacks, median correctness proved by counting listings on each side, city resolution, no "sold" wording anywhere), **17/17 in a real browser against `/crm`** (panel renders, Pending flagged, own-home not repeated, empty lead collapses to zero height, no JS errors), **10/10 on Harvey's `get_lead`**, plus the existing 29/29 and 15/15 suites still green. **HONEST STATUS — none of these emails can currently be delivered.** `/api/email/connection-status` returns `Gmail token refresh failed: invalid_grant`, `/api/email/stats` shows 0 sent, and there are **0 active drip sequences**. The wiring is real and tested; the Gmail refresh token needs re-issuing before a single one of these goes out. The CRM panel and Harvey's `get_lead` are live and unaffected by that.

- [2026-08-02] — **Live MLS facts injected into the REAL DM pipeline (Instagram only), after finding the numbered modules are dead code** (`src/integrations/llm/index.ts`, `src/core/listingMatch.ts`, `src/modules/05-property-breakdown/index.ts`). **Correcting the previous entry: wiring module 05 did not put MLS data in front of a single lead.** `src/modules/05-property-breakdown` has **zero external references** — and so do modules 02, 04, 06 and 07. The live DM flow is a single unified LLM prompt assembled in `integrations/llm`, with `prompts.propertyBreakdown` folded in as tone guidance only. Rewriting the module produced correct code that nothing calls. **The facts now go where the model actually reads them:** a new `LIVE_MLS_FACTS` hint in `postOpeningHints`, carrying address, price, beds, baths, sqft, status and MLS number for the matched property plus real comparables, with an instruction to **copy every figure exactly, never round or re-estimate, and never state a number not listed**. **INSTAGRAM ONLY, and that is a compliance rule, not an oversight.** The very next hint is `TIKTOK_NO_LIST_PRICE_IN_DM` — TikTok DMs must never quote a list price — so supplying real figures there would hand the model precisely what it is forbidden to say. Blindly injecting everywhere would have broken that; the guard is asserted by test. Matching keeps the earlier discipline: `matchListingForLead` returns only `exact` or `none`, needs an MLS number (6+ digits, so a budget or zip cannot pose as one) or a street number AND name resolving to **exactly one** listing, and verifiably refuses *"anything on Rockcress Rd?"* because that street is a row of new builds. Without an exact match the hint explicitly forbids "the home you asked about" and the listings are offered as options. A lookup failure is caught so a lead always still gets a reply. Module 05 keeps its corrected implementation (including a rewrite-integrity guard whose SET-based version let an altered bed count through when a figure appeared twice, now count-based) — but it is **dead code**, and this entry says so rather than leaving the next reader to assume the funnel runs through it. Verified **15/15** on the pipeline injection (hint present, TikTok gated off, exact-copy instruction, no-invention instruction, the no-match wording ban, non-Active status surfaced first, lookup failure non-fatal) plus **29/29** on the matcher and reply builder against the real 27,083-listing mirror. **Test note:** assert on the compiled LINE, not on a fixed emit shape — tsc namespaces imported symbols (`(0, index_js_1.isMlsFeedConfigured)()`) but leaves local ones bare (`isTikTokPlatform(...)`), so a regex pinning both to one form fails on working code.

- [2026-08-02] — **The DM funnel now answers with real MLS prices instead of promising a breakdown it never delivered** (`src/modules/05-property-breakdown/index.ts`, `src/core/listingMatch.ts` (new)). Closes the gap the 08-02 audit flagged: this module told leads *"here's the breakdown on location, specs, pricing, and a couple of other options"* and then sent none of it, because there was no MLS. **101 leads reached that stage and got it.** It now quotes live SABOR figures. **RULE 1 — a wrong price is worse than no price.** `matchListingForLead` returns only `exact` or `none`; there is deliberately no middle tier, because a "probably" would end up quoted like a certainty. `exact` requires an MLS number (6+ digits, so a budget or a zip cannot pose as one) or a street number AND name resolving to **exactly one** listing. Verified against the real mirror that *"anything on Rockcress Rd?"* does NOT resolve — that street is a row of new builds, and picking one would have sent a buyer the wrong house's price. On `none` the lead gets real comparables and the copy never says "the home you asked about". **RULE 2 — the tone rewrite must not touch the numbers.** The reply is built deterministically, passed through Marco's rewriter for voice, then **rejected if any price, bed/bath count, square footage or MLS number changed**, falling back to the exact text. An LLM rounding $534,149 to $535,000 has invented a price for a real house and sent it to a real buyer. **The test found a real hole in that guard.** It first compared facts as a SET, so when a reply mentioned "2 bed" twice (the matched home and a comparable) and a rewrite altered only the first, the second still satisfied the check and the change passed. It now compares **occurrence counts**. That is exactly the mutation a rewriter makes, and the set version would have shipped. Comparables are anchored on the matched listing (same city, ±25% price) or the lead's own criteria, and return **nothing** rather than random homes when there is nothing to anchor on. A non-Active listing leads with its status, because on a house already under contract that matters more than the price. Feed unconfigured, lookup throws, or nothing matches: falls back to the original script, so the funnel never breaks. Verified **29/29 against the real 27,083-listing mirror**, adversarially rather than on happy paths: a vague "that house you posted" produces no match, a 6-digit budget is not read as an MLS number, a phone number is not either, and four separate rewrite mutations (rounded price, dropped price, altered beds, altered sqft) are each rejected.

- [2026-08-02] — **MLS is LIVE (27,083 SABOR listings) — plus an audit of where that data does and does not reach** (`public/listings.html` (new), `src/server.ts`, `public/shell.html`). Backfill finished: **27,083 listings, 23,205 Active / 3,878 Pending**, mirror complete, syncing every 30 min. Harvey answers real market questions from it (verified: "186 active in Boerne 400-600k with 3+ beds" with addresses, prices and MLS numbers). **AUDIT RESULT — the data was live in 2 of 7 systems.** Wired: Harvey's tools, the `/api/mls/*` endpoints. **NOT wired, each confirmed by grep at zero references:** the DM pipeline, any UI, the CRM/tracker/lead record, email marketing, the content manager. **Closed the biggest one: there was nowhere to SEE the listings.** New `/listings` page (plain HTML/CSS/JS, no build step, matching the jobs/tracker style) with photo cards, price/beds/baths/sqft, status and MLS number, and filters for city, status, price, beds and baths. It surfaces feed health FIRST — listing count, hours since sync, and an explicit banner when the mirror is mid-backfill or stale — because a page that renders an empty grid without saying the feed is down is how "no listings" gets mistaken for "no market". Registered at `/listings` and added to the shell nav as **MLS**. Verified **11/11 in a real browser against a copy of the production listings.db**: the real 27,083 count, 60 cards rendered, page-size AND true total shown separately ("Showing 60 of 23,205"), real prices/addresses/MLS numbers, filters returning exactly the 186 the API reports, and every result genuinely in Boerne. **Two harness artifacts worth recording:** Chrome has no outbound network in the sandbox, so `networkidle` never fires (the page requests MLS CDN photos) — use `domcontentloaded` and abort external requests. And every `<img>` then fires `onerror`, so the page swaps in its "no photo" tile and an `img.photo` selector finds nothing; the photo URLs are real (**99% of listings carry a connectmls CDN URL**) and are asserted against the API rather than the post-error DOM. **STILL NOT WIRED, and the biggest of these is outward-facing:** `src/modules/05-property-breakdown/index.ts` still sends a generic *"here's the breakdown on location, specs, pricing"* script with no property data behind it — **101 leads have reached that stage and received it**. Its comment still reads "v0: no MLS/MLS integrations yet", which is now false. Changing it alters what real leads receive over DM, so it is left for an explicit decision rather than shipped quietly. Also unwired: no `listingKey`/`mlsNumber` field on a lead (so `propertyInquired` stays free text — 22 leads all carry the same hardcoded "Canyon Lake, TX — 3/2 $365k"), email drips, and the content manager. **Sizing note:** `listings.db` is **263 MB** for 27,083 records, since the whole raw SimplyRETS record is kept per listing. That is 1.3% of the 20 GB volume, so fine now, but it scales linearly if more vendors are added.

- [2026-08-02] — **MLS integration built against Bridge Interactive (Zillow), RESO Web API — SHIPS DORMANT until credentials are supplied** (`src/integrations/bridge/index.ts` (new), `src/core/listingsStore.ts` (new), `src/agents/mlsSync/index.ts` (new), `src/harvey/tools.ts`, `src/app/jobs.ts`, `src/server.ts`). There was no MLS integration at all — `src/modules/05-property-breakdown/index.ts:15` said so outright (*"v0: no MLS/MLS integrations yet"*) and sent every phone-captured lead a generic scripted "here's the breakdown" with no property data behind it. No listings store existed; a property was a free-text `propertyInquired` string. **Bridge is a RESO Web API (OData v4) feed**: `GET {base}/{dataset}/Property` with a Bearer **server token**. New `listings.db` mirrors it (own file, standard resolve-path/lazy-singleton/init-schema pattern). **RESO field spellings are kept verbatim** (`ListingKey`, `StandardStatus`, `ListPrice`) rather than prettified, because renaming means maintaining a translation table against a spec that changes yearly; the **untouched raw record is stored alongside**, since MLS feeds carry hundreds of board-specific fields and the unmapped ones are exactly what gets asked about later. **Sync is incremental**: each run filters `ModificationTimestamp gt <newest held>`, so the first pull is the only expensive one and a 30-min cadence is affordable on a metered feed. Runs are capped (5,000 first / 2,000 after) with **truncation reported rather than silent**, so a partial mirror can never masquerade as complete. Every attempt is recorded, successes AND failures — a feed that quietly stopped updating is indistinguishable from a quiet market without a visible last-successful-sync time. **The honesty rule this is built around:** an unconfigured feed returns `{ok:false, notConfigured:true}` naming the exact missing env var, never an empty listing array, and Harvey's tools carry an explicit `say` forbidding "no listings found" and "that property doesn't exist" when the feed is simply off. Staleness is reported in HOURS, not as a green tick, because "is the MLS working" has a different answer at 2 hours and at 11 days. Three Harvey tools (`search_listings`, `get_listing`, `get_mls_status`) and four endpoints (`/api/mls/status`, `/api/mls/ping`, `/api/mls/sync`, `/api/mls/listings`). `/api/mls/ping` proves the credentials by **actually calling Bridge and returning the field names it sends**, not by checking that an env var is non-empty. Verified **32/32 against a mock that behaves like the real API** (OData `@odata.nextLink` paging across 250 records, Bearer auth, 401 on bad token, 404 on bad dataset): missing token/dataset each named individually with the fix, Bridge's own error message surfaced (a 401 explicitly says a client secret will not work, since that is the mistake people make), paging followed, runaway cap honoured and reported, RESO→row mapping with address assembled from parts and photo pulled from `Media[]`, upsert idempotent, every search filter, incremental high-water mark, and both Harvey not-configured paths. **NOT LIVE YET, and deliberately not faked.** `pmarco_96z6805b` is Bridge's **client ID** format, not a secret. Still required: `BRIDGE_SERVER_TOKEN` (the Server Token from the Bridge dashboard — the client secret will not authenticate) and `BRIDGE_DATASET` (the board dataset name). Until both are set, `scheduleMlsSync()` logs that it is not scheduled and every tool says the feed is off. **What the mock cannot prove is that Bridge's live field spellings match ours** — that is what `/api/mls/ping` is for, and it should be the first thing run once the token exists.

- [2026-08-02] — **Fixed the ManyChat unsubstituted-merge-field bug at ingestion, and repaired the 35 leads it was still costing** (`src/app/webhook.ts`, plus a one-off data repair). When a ManyChat flow references a subscriber field that is empty or misnamed, it posts the token verbatim — `{{full_name}}`, `{{ig_username}}`, `{{tt_username}}`. **Nothing in the pipeline checked**, so the token became the person's identity. Surfaced when lead scoring started working and a lead nobody could name or phone ranked FIRST on the call list. **The serious half was not cosmetic.** For 2 leads the token landed in `user_id`, the identity KEY, so every sender with that payload collapsed into one lead: **lead 109 holds 18 messages across seven weeks from people asking about different listings** ("price of this luxury home", "this home with low intrest", "Too far unfortunately"). Those were separate prospects sharing one thread and getting replies written for someone else. **The guard lives in `normalizeWebhookRecord`**, the single choke point all five `pick*` helpers read from — five pickers each validating separately is five chances to add a sixth that forgets. A token is dropped from identity fields, is never allowed to fill an empty slot during the `data`/`contact`/`subscriber` merge (where it would then be preferred over a real value on a later pass), and is scrubbed from the top-level body too. **A token in `user_id` now REJECTS the payload with 400** and an explicit log rather than creating a lead: losing one message is recoverable, a thread merging strangers is not, and a 400 tells ManyChat something is wrong instead of failing silently. **The message body is deliberately NOT scrubbed** — a DM containing a stray token is still a real message from a real person, and dropping it to fix a cosmetic problem would lose the conversation; it logs a warning instead. Verified **17/17 through the real webhook entry point** using the three token shapes actually found in production, asserting what landed in the lead store rather than that the request returned 200: the production payload shape is accepted with the tokens stripped and the message kept, a genuine full name is untouched (no false positives), nested `contact{}`/`subscriber{}`/`data{}` shapes are covered, a real name under `subscriber{}` survives a token in `data{}`, a token `user_id` is rejected with no lead created, and **two such payloads no longer merge into one lead**. **Data repair:** 35 leads had the token in `name` but their real handle (`trent_miller__`, `swt.life`) sitting unused in `user_id` — the identity WAS captured, just not into the displayed field. Copied across via `PATCH /api/crm/lead/:id`; non-destructive, fills a blank rather than deleting anything. Zero token names now remain in any tier. **The 2 merged leads were left alone and reported as unrecoverable** — there is no record of which message came from whom, so the threads cannot be split and the script does not pretend otherwise.

- [2026-08-02] — **Lead scoring reweighted around signals the system actually captures; hot is reachable for the first time** (`src/agents/leadScoring/index.ts`, `src/core/leadScoreStore.ts`, `src/core/db.ts`, `src/server.ts`, `src/harvey/tools.ts`, `public/lead-nurture.html`). Follow-on from the 08-01 audit, which found the model could not produce a hot lead: 50 of 100 points sat on `timeline`/`preApproval` (manual CRM fields the type itself documents as manual, and nobody fills), 30 more on property views and booked showings the DM funnel never emits. Ceiling 20/100 against a hot threshold of 80, so **all 1,306 leads were cold forever and "0 hot leads" was an artefact of the scorer, not a fact about the business.** **New weights — engagement 30, funnelDepth 25, recency 20, contactable 10, intentSignals 15.** The first four are captured automatically on every lead and sum to **85**, so hot is reachable with zero manual data entry; the old manual fields survive as a single 15-point bonus, so filling them still helps and their absence no longer flattens everything. `STAGE_INTENT` ranks stages by what they SAY about a lead rather than sequence position (handing over a phone or stating criteria outranks being asked a question), and `closed` scores 0 because this score answers "who does Carlos call next" and a finished conversation is not a call. **Recency is entirely new and was the missing decay:** 741 leads had sat stalled since spring looking exactly as warm as the day they went quiet. Uses the last INBOUND message (via a new sync `getLastInboundDmAt`), not the last touch, because five unanswered follow-ups we sent are not interest. **Result on production's real 1,306 leads: 2 hot, 316 warm, 988 cold, 48 distinct scores** (was 0/0/1306, single value). Top of the list is a lead with 11 replies at `criteria_collected` who answered yesterday and has a phone. Fresh leads average 56.3 vs 34.6 for stale — the ranking now moves. **Made it STAY correct, which mattered as much as the weights.** (1) Rescoring ran only if the process happened to be alive during a **two-minute wall-clock window at 09:00 Central**, behind a **thirty-day** gate, tracked by an in-memory flag — this app deploys several times a day, so a restart in that window skipped the day entirely; scores written 25 June were still being served as current in August. Now: a pass ~2 min after boot, then every 6h, gated on the persisted last-run time. No window to miss, and a deploy makes scores fresher instead of skipping them. A monthly refresh was in any case incompatible with a model that includes recency. (2) It rescored **cold leads only**, so a hot lead could never cool; now every lead. (3) `scoreAndRecordLead` **skips the insert when score and tier are unchanged** — at 4 passes/day an unconditional write would add ~1.9M rows/year of "nothing happened"; now a row means the lead actually moved. **Killed three copies of the weight table** (`server.ts` factorMax, `NURTURE_WEIGHTS` in harvey tools, and the Lead Nurture UI) — all still described the pre-reweight factors, which is exactly how a duplicated constant drifts. Server and tools now import `WEIGHTS`; the UI detects which model a row was scored under and renders the legacy factors for historical rows rather than five empty bars. `ScoringFactors` fields are optional so pre-reweight rows still parse. Verified **43/43 against a copy of production's real lead database** (18 scoring + 25 visibility regression). **Two assertions earned their keep:** the previous suite's deliberate "KNOWN LIMIT: tiers are unreachable" check fired the moment the weights changed, exactly as designed; and a ">90% of warm leads are callable" assertion failed at 76% and was **wrong, not the model** — a lead who replied ten times without giving a number is genuinely warm and the next action is to ask for it over the live DM thread, so the invariant is now "reachable by phone OR active DM" (318/318).

- [2026-08-02] — **Fly bill cut from $79.79 to ~$16/mo for the machine (~66-80% off), sized from measurement** (`fly.toml`). Audit of an $84.91 bill. Nothing was spiking: 86,400 units/day is exactly 86,400 seconds, so it was one machine billed for 100% uptime. The cost was the SIZE. Reconstructing the bill from Fly's component rates matched the invoice to **0.2%** ($57.89 dedicated CPU + $21.77 RAM = $79.66 vs $79.79 actual), which is what made the savings figure real rather than a guess — a first attempt using a wrong RAM rate came out 101% off and was caught by checking it against the actual bill. **73% of the bill was CPU measured at 0.81% utilisation**: over 26.7h the whole machine used 1,561s of 192,212 available core-seconds, load average 0.00/0.00/0.00. Dedicated CPU costs ~14x shared and nothing here needed it. Now `shared-cpu-8x` + 2GB + 1GB swap: **four times the cores** for the bursty video path, against a measured 803MB peak (VmHWM) across all three services. **Two things this audit caught that mattered more than the resize itself.** (1) `fly.toml` still said `performance-2x`/`4gb` after the machine was resized, and **`flyctl deploy` applies that block — the next deploy would have silently re-inflated the bill.** The config is now the source of truth with a comment saying so. (2) `swap_size_mb` was first placed after `[http_service]`, where **TOML attaches a bare key to the preceding table header** — it became `http_service.checks.swap_size_mb`, `flyctl config validate` still said "Configuration is valid", and the machine came up with `SwapTotal: 0`. Only checking `/proc/meminfo` on the running VM caught it; it is now top-level, above the first table. Swap is the reason 2GB is sensible rather than brave: the pipeline does render (4 clips in the last 30 days) and ffmpeg can spike, so the worst case is now SLOW rather than an OOM kill that would take the ~30 scheduled agents down with it. **The machine cannot be allowed to sleep** — `min_machines_running = 1` looks like it defeats `auto_stop_machines` but is correct, because every scheduled agent runs on a boot-time `setInterval`. Size was the only lever. Verified after each step: 8 cores, 1GB swap active, all three supervisord services back (Node, OpenShorts, CapCut), health green, Harvey answering, 746MB used of 1,967MB with 2,245MB total headroom. Not touched: the 20GB volume is 99.8% empty (45MB used) but Fly volumes cannot shrink in place, so reclaiming ~$2.45/mo would mean migrating live data — not worth the risk.

- [2026-08-01] — **Audit: what Harvey can actually see, and four fixes where he was WRONG rather than merely missing** (`src/harvey/tools.ts`, `src/core/db.ts`, `src/agents/leadScoring/index.ts`, `src/server.ts`). Asked to make sure Harvey has access to all the leads and systems. Full inventory: **100 tools** spanning leads, tracker (1,219 records), tasks, transactions, finance, content, social, email, reporting, knowledge, browser, workspace and memory. Coverage is genuinely broad; the problems were not breadth. **(1) Lead scoring was dead and reporting it as fact.** `scoreResponseCount` read `sms_threads` ALONE, but Twilio is unconfigured in production (that table is empty) while the entire funnel runs on Instagram/TikTok DMs. **781 of 1,306 leads (60%) had replied over DM and scored 0 for engagement** — a lead who answered 51 times ranked identically to one who never answered. Now takes `max(sms, dm)` via a new sync `getInboundDmCount()`; the higher of the two rather than the sum, because a lead who moved from DM to text is one conversation. **KNOWN LIMIT, deliberately not silently fixed:** warm needs 40 and hot needs 80, but `timeline` (25) and `preApproval` (25) are manual CRM fields nobody fills and `propertyViews`/`showingRequests` (15+15) need signals the DM funnel never emits, so **only 20 of 100 points are reachable and every lead stays cold**. Re-weighting decides who Marco calls first, so it is his decision, not a quiet code change. A test asserts this limit and fails loudly if weights change. **(2) `get_conversation` silently showed half the relationship.** It returned DM messages only, so an SMS-only lead came back as a short, confident, empty-looking thread with no hint an entire text history existed. A missing tool announces itself; this did not. Both channels now merge in time order, every line labelled `[DM]`/`[SMS]`, with `dmCount`/`smsCount` returned separately and an explicit say-so when there is genuinely no history. **(3) 1,601 orphaned score rows.** `lead_scores` held 2,902 scored ids against 1,306 real leads because ids churn on re-import and scores are never cleaned, producing "cold: 2902" on a board of 1,306 — and `Math.max(0, unscored)` clamped away the negative that would have exposed it. Summary and tier-detail now count only scores whose lead still exists. **(4) Harvey could count a segment but never work it.** `search_leads` took a name/phone string only, so he could report 101 leads at 'property sent' and name none of them. Now filters on `stage`/`source`/`hasPhone` with no query required, and returns `totalMatching` beside `count` so a page is never mistaken for the whole list. **Two new tools closing the biggest data holes:** `get_sms_thread` (sms.db held every text ever sent or received and NO tool read it — only two derived numbers leaked out) and `get_my_notes` (`noteCapture` has been writing harvey-notes.json all along with nothing to read it back, so Harvey confirmed saving notes he could never retrieve). Verified **25/25 against a copy of production's real 1,306-lead database**, not fixtures — the fixture version of this test would have passed before any fix. **The test caught a real bug mid-flight:** the `search_leads` executor was updated while its tool SCHEMA was not, so the filters existed and the model could not invoke them. **Remaining gaps, unbuilt and worth knowing:** no tool for ad spend (`ads.db`), power-dialer sessions, voice clone, team-chat message bodies, auth/audit trail, drip membership, document templates, CRM/Marco task stores, or Brivity import freshness; comment-reply queue is read-only; and the Gemini path (`HARVEY_GEMINI_TOOLS`) exposes ~46 tools, losing the tracker, task board, knowledge center and browser entirely.

- [2026-07-31] — **The task board shows who assigned what to whom** (`public/team-tasks.html`). Asked for after Harvey started writing tasks: a card said only *Carlos* with no hint that Harvey put it there, so a task's origin was invisible on exactly the surface where it matters. The data was never missing — every task has carried `createdBy` all along (the page's own `canEdit` and the "X assigned you a task" notifications already used it) — the board just never showed it. The assignee chip is now an assignment chip: **"Ⓗ Harvey ➜ Ⓒ Carlos"** when someone put work on someone else's plate, collapsing to the familiar plain chip when self-assigned (a self-arrow is noise) and for legacy tasks with no recorded creator (show nothing rather than guess). Non-human creators get their own honest identity via a small `BOTS` map — `harvey` renders as **Harvey** in his cyan, `automation`/`system` as **Auto** in slate — rather than being dressed up as a person. The edit modal also gained a read-only *"Assigned by Harvey · July 31, 2026"* line. Deliberately display-only: no store change, no API change, and the server's `createdBy` default (`"carlos"`) was left alone because the assignment-notification path keys off it. Verified **10/10 in a real browser** (chip text, both avatar colors computed from the render, hover title, self-assigned collapse, Auto for the reminder agent, modal line), plus eyeballed screenshots of all three chip states. **Testing note for the next agent:** `/team-tasks` now bounces through the `/who` sign-in gate; driving the gate's button races `location.replace` and makes the page look script-dead. Preset the `mp_team_session` cookie + `marcoTaskUser` localStorage key instead.

- [2026-07-30] — **Follow-up, found by testing against the LIVE model: the ban on the em dash just moved the tell to `--`** (`src/core/houseStyle.ts`). The first real job run after the rule shipped came back with *"Call these three first -- they have real names"* and *"Pete Morales -- (512) 736-6961 -- San Antonio number"*. The em dash was genuinely gone and so were the other tells (the title was a plain *"Call top Contacted-stage buyers by phone"*, no ALL-CAPS, no emoji, no "good news"), but the model had **routed around the ban rather than restructured the sentence**. This is the argument for the code layer stated out loud: a prompt rule tells the model what not to type, and a model that wants a pause will find another character to type it with. Both layers now cover the ASCII double hyphen. The prompt says the fix is *"not a different character, it is a different sentence"* and carries a WRONG/RIGHT pair for it. The sanitizer treats `--` as a dash **only** when spaced or between two non-spaces, and skips any line that is nothing but dashes, pipes, colons and spaces, so a CLI flag (`--permission`, `--allow-fs-read`), a markdown horizontal rule, and a table separator (`|---|---|`, `|:--|--:|`) all survive untouched. Suite is now **48** on the sanitizer (up from 35) and 17 on the wiring. **Worth knowing for next time: this was only caught because the deployed system was asked to write a real task. The 52 offline checks all passed on a build that still had the hole.**

- [2026-07-30] — **Harvey writes like a person, not like an AI** (`src/core/houseStyle.ts` (new), `src/hull/jobRunner.ts`, `src/hull/founderPrompt.ts`, `src/harvey/platformTools.ts`, `src/hull/agentLoop.ts`, `src/harvey/index.ts`, `src/core/db.ts`). Marco, looking at a task Harvey had just put on his board: *"a lot of m dashes and, like, a lot of those AI type looking features... let it be as human as possible."* The task read `"Chase top 10 Contacted buyers — get them on the phone"` / `"All 228 buyers in Contacted stage actually HAVE phones — good news."` / `"1. Raul Trevino — (210) 718-3874 — STATUS: HOT 🔥"`. **The root cause was structural, not stylistic: `jobRunner.ts` had NO persona layer at all.** The voice rules live in `buildFounderSystemPrompt`, which only the interactive loop calls, so every background job — the surface that writes the most prose, and the one that wrote this task — ran on a 13-line prompt with no tone rules whatsoever. **The DM side of this codebase had already solved this exact problem and Harvey never got the fix:** `GLOBAL_MARCO_DM_RULES` bans the em dash in the prompt AND `enforceOutboundTextRules()` strips it in code, a second layer that exists because (per its own doc comment) prompt-only enforcement is not reliable — the ban was already in the prompt when a live lead received *"Yeah, of course — is there a good number"*. So this reuses that proven two-layer shape rather than inventing a third one. `HARVEY_HOUSE_STYLE` is INSTRUCTION (voice cannot be regex'd): it bans em/en dashes and the spaced-hyphen substitute, emoji status markers, ALL-CAPS emphasis, editorialising asides ("good news", "here's the thing"), the "not just X, it's Y" construction, and closing summaries — and says what to do instead, because a pure ban list just produces a new kind of stilted. It is now injected into BOTH the interactive prompt and the job prompt. `stripAiTypography()` is a GUARANTEE: mechanical, meaning-preserving, runs after the model on task titles/bodies and job reports. **Only the punctuation half is guaranteed; "sounds human" is steered, and the module says so rather than overclaiming.** It is deliberately NOT the DM version reused verbatim — that one replaces `[—–]\s*` and so eats the following newline, which is harmless in a one-line SMS and would concertina a multi-line task body into a paragraph. **Three things it must not break, all tested:** a number range (`6–8` → `6-8`, not a comma), a dash used as a bullet (→ `- `, not `, `), and a dash at end-of-line or against punctuation (deleted, not left as a dangling comma) — the last needs a sentinel so cleanup only ever touches text the function itself changed, or legitimate trailing commas would be stripped from lines that never held a dash. **Scoped to Harvey's write path, never a human's:** it is applied in the `create_task`/`update_task` handlers (which stamp `createdBy: "harvey"`), not in `createCommandTask`, because Carlos genuinely types em dashes and silently rewriting his words would be a worse bug than the one being fixed. It is also NOT applied to workspace files, because a `.csv`/`.json` may hold a dash inside a real data value and rewriting that is corrupting data, not fixing style. **The prompts themselves were rewritten to stop modelling the habit they now ban** — `founderPrompt.ts` was em-dash-heavy throughout (`"Marco Puga — luxury real estate agent"`, `"Ops partner tone — lead with the number"`) and a model imitates the register of its own instructions; that file is now at zero. `create_task`'s schema had **no guidance on writing a task body at all**, an unfilled slot, and now carries the rule at both tool and field level. Seed fixtures in `db.ts` fixed too. Verified **52/52**: 35 on the sanitizer (the real production task, every dash position, the must-not-change cases, curly quotes/ellipsis, a markdown table, and the prompt block practising what it preaches) and 17 on the wiring — including that an em dash cannot survive a real `create_task` call, that `update_task` cannot smuggle one back in, and that a task created by a human keeps its punctuation.

- [2026-07-30] — **Harvey can read the web, through the paired browser instead of a search API** (`src/core/webResearch.ts` (new), `web_search` rewritten and `read_web_page` added in `src/hull/tools.ts`, `/__test/tool` widened in `src/server.ts`). This is **Phase 1 of `docs/harvey-recurring-research-scope.md`**, the blocker under the whole recurring-research vision: `BRAVE_SEARCH_API_KEY` is unset in production, so `web_search` was **not even registered** and Harvey had no way to look up anything outside Marco's own data. The operator chose the browser path over buying a key, and **it needs no extension change** — it composes actions the installed MV3 extension already understands (`navigate`, `waitFor`, `extract`, `read`). The primitive that makes it possible is `pageExtract`'s `"a.result @href"` attribute syntax, which returns hrefs already resolved to absolute URLs; without that, server-side result scraping would not work at all. **Two engines, tried in order** (DuckDuckGo's no-JS HTML endpoint, then Bing) because one engine is a single point of failure against markup that changes without notice. Each engine's redirect wrapper is unwrapped to the real destination (DDG's `/l/?uddg=`, Bing's `/ck/a?u=a1<base64url>`). **The rule this module exists to keep: an empty result set and a broken scraper look identical from the outside, and only one of them means "nothing was found."** So when every engine yields nothing the response is an explicit **SCRAPE FAILURE** naming the engines tried, with a `say` forbidding "nothing found" — a silent markup change must never become a confident wrong answer. `read_web_page` is the half a search API cannot do at all: **snippets are not sources**, and this returns the page's actual body (capped at 14k chars, truncation declared). Because it is Marco's own signed-in Chrome it can also read pages behind a login — and when it lands on one, `needsLogin` is set and Harvey is told he received *the sign-in page, not the source*, rather than summarising a login wall as the article. `web_search` still **prefers Brave if a key ever appears** (it runs unattended); the browser is the fallback. **Found and fixed a real product bug while testing:** the extension creates its work window lazily on the first command, so the very first `navigate` of a session could exceed the server timeout or come back "No page open to act on yet" — a research task dying on its opening move. `navigateWarm()` retries once, but **only** on transient-looking failures, and reports the *original* error rather than the retry's echo. Search-engine internal links are filtered **by host, not by path**: pattern-matching the paths an engine uses today misses pagination, "more results" and related-search chips, and each one that slips through becomes a URL Harvey may try to read and cite. Verified **23/23 through the real extension in a real Chrome** — nothing mocked between Harvey and the page, the extension polling the real server against locally-served fixtures faithful to DDG's structure (redirect wrapper unwrapped, internal nav filtered, `limit` honoured, the page body returning *both* figures a snippet would not have, the login wall flagged, the scrape-failure path naming both engines, bad input refused). `/__test/tool` now routes through `executeHullTool` rather than the narrower platform dispatcher — testing a different path than production runs is how a tool passes its tests and fails in the operator's hands. **The honest limit, unchanged: this needs Marco's Chrome open and armed.** It cannot power an unattended 3am job. Phases 2–4 of the scope doc (the briefs store + scheduler, the living-document convention, the briefs surface) are still unbuilt, and until there is an API backend a nightly brief will only run when the browser happens to be there.

- [2026-07-30] — **`/jarvis` transcript grows when there is something to read; fixed a 5px composer overlap** (`public/jarvis.html`). `.j-transcript` was capped at `max-height: 16vh` — correct for a voice console where the text is a receipt for something spoken, far too small now the command bar is back, since a job report is thousands of words and reading it through a thin band is worse than not showing it. A `body.j-has-chat` class now grows the pane to `min(56vh, 620px)` and **shrinks `.j-core-wrap` from 70vmin to 34vmin**, because the orb is what was holding the space — capping the transcript alone would only have made it scroll. Measured: transcript **16px → 193px** for a short reply and **560px** with a job card, orb **580px → 340px**, and it all reverts when the transcript is cleared. On viewports under 700px tall the orb yields further (22vmin) so both still fit rather than squeezing each other. The class is driven from `addMsg` and the job-card mount, and counts only real `user`/`ai`/job-card messages — the boot placeholder and status lines are not content, and growing for those would shrink the orb on every page load for nothing. **Bug found by the pixel assertions rather than by eye:** `--j-compose-h` was hardcoded at `54px` while the composer actually renders **59px**, so `.j-main` (`bottom: var(--j-compose-h)`) ended 5px too low and the transcript sat *under* the input on every screen size. Invisible while the pane was a 16vh strip; not invisible once it reaches the bottom. The variable is now measured from the composer on load, on resize, via `ResizeObserver`, and after `document.fonts.ready` (web fonts land after first paint and change the input's height) — which also covers the safe-area inset on a phone. Gap is now −1px. Verified **17/17** on growth, all measured in real pixels rather than by class name, since a class that toggles while nothing moves would pass a naive check and fix nothing; prior suites re-run green (18/18 composer, 30/30 three surfaces, 21/21 Jobs page).

- [2026-07-30] — **Restored the typed command bar on `/jarvis`** (`public/jarvis.html`). The composer was removed in `b868746` when the console went voice-first, but the removal was incomplete: `sendMessage`, the `#jForm` submit handler and four `#jSend` references stayed behind as **dead code that could never run**, and `--j-compose-h: 54px` was still reserving an empty strip at the bottom of the page. Recovered the original markup from `7cf01bc` (`#jForm` / `#jInput` / `#jSend`) rather than reinventing it — all the CSS (`.j-compose`, `.j-input`, `.j-send`, `.j-prompt`, plus the light-theme overrides) had survived untouched, so this is markup going back into styling that was already waiting for it. The submit handler's optional-chaining guard is kept deliberately: the composer has been deleted once already and the handler should not throw if it happens again. With this, a typed job on `/jarvis` now gets the shared job card in the transcript, so the surface is no longer voice-only. Verified 18/18 by actually typing into it: form/input/button present and on screen filling the reserved 54px, Enter and the SEND button both submit, the message is sent with `full:true` and appears in the transcript, the input clears, Harvey's reply renders, a job id produces a live card that reaches Done with its file offered, and — the failure paths — a 500 is shown in the transcript rather than swallowed, **Send is re-enabled after a failure instead of being left permanently disabled**, and an empty submit does nothing. Prior suites re-run green (30/30 across the three surfaces, 21/21 on the Jobs page). **Known limit, unchanged by this:** `.j-transcript` is capped at `max-height: 16vh` because the page is built around the voice orb, so a long reply or a multi-page job report is readable only by scrolling a thin band. Fine for voice, cramped for typing — worth revisiting if typing becomes the main way this page is used.

- [2026-07-30] — **Job handoff on every Harvey surface, from one shared module** (`public/harvey-jobwatch.js` (new), `public/hull-chat.html`, `public/jarvis.html`, `public/operator.html`, `public/aethon-voice.js`, `public/jobs.html`). Asked to port the chat job card to `/operator` and `/jarvis`, and **found both are voice-first with no text composer**: `/operator` is an orb and a status pill with no transcript at all, and **`/jarvis`'s text-chat path is dead code — `#jSend` does not exist in its DOM**, so its `postHarveyChat` branch can never run. A chat bubble has nowhere to go on either. Both can still START a job by voice, so both had the same defect on a surface where the bubble fix does not apply. **New `harvey-jobwatch.js` owns polling, the card, the markdown renderer and the file rows; each page only says WHERE a card is mounted.** Copying it would have made three implementations of the same rules — the exact shape of the duplicated-CSV-mapping bug that wrote to the wrong store — so **all four pages were converted to the one module**, including `jobs.html`, which had its own fourth copy. `hull-chat` also now uses it for ordinary replies, so the chat and the card cannot render the same markdown differently. Mount modes: a chat bubble (hull-chat), the HUD transcript (jarvis), and a new **floating dismissable notice** for a page with no transcript (operator). The voice path is hooked once in `aethon-voice.js`, which both HUD pages load, so a job started by voice is followed to completion too. Self-styling with a `body.hjw-light` variant so a light page (`/jobs`) and a dark console share one stylesheet. Verified **96/96** across four suites: **30/30** on all three chat surfaces (live card → Done, report rendered with headings and a real table, zero raw pipes, file offered with Download/Open, internal run artefacts excluded, plus the floating notice's ✕ surviving each card rewrite), **21/21** on the Jobs page after the swap, **15/15** on deletion, **30/30** on the code-execution runner. **Two things the tests surfaced about Harvey's own behaviour, both correct:** the "review all 1,219 records one at a time" job (test 6) was **refused** rather than run — *"It would overwrite real notes on 1,219 records… `update_tracker_record` REPLACES whatever is already there"* — and a job that only READ a file correctly shows no "files produced" card. **Known limit:** on `/operator` and `/jarvis` the job card can only appear for work started through the voice path or the API, because neither page has anywhere to type; restoring a text composer to `/jarvis` is a separate job.

- [2026-07-29] — **The chat follows a background job through to the end; jobs and files can be deleted** (`public/hull-chat.html`, `public/jobs.html`, `DELETE /api/harvey/jobs/:id`, `DELETE /api/harvey/workspace`, `deleteJob()` in `src/core/jobStore.ts`). **The reported bug was "test 4 failed, it's been over 10 minutes and it's stuck". The job was not stuck — it finished in 1m 43s after 34 tool calls and wrote a 12.3 KB report.** Harvey self-escalated correctly and replied with a job id; the chat then had **zero** references to the jobs API, so it could not tell that anything had happened. A job that completed in 100 seconds looked identical to one that had hung. **Self-escalation without a handoff back is not delegation, it is a dropped thread.** The chat now watches any `job_…` id in Harvey's reply and reports the outcome in the conversation: a live card naming the current step in operator language ("searching the tracker", not `search_tracker`), then flipping to **✓ Done** with the report rendered and the produced files as Download / Open rows. Polling is deliberately long-running (~20 min) and, if it does run out, says so and points at `/jobs` rather than going quiet — giving up early and leaving a "still working" card is the exact failure being fixed. Internal run artefacts (`script.js`, `guard.cjs`, `output.txt`) are filtered out of the offered files. **Found while testing this: the chat's markdown renderer only handled bullets, bold and code fences** — no headings, no tables — so Harvey's structured replies rendered as a wall of raw `###` and `|`, the same "codey" complaint that prompted the Jobs redesign. It now matches: headings, tables, hr, ordered lists, blockquotes, escaped-first. **Deletion**, as requested: an ✕ on every job row and every workspace file. A **running** job is *stopped*, never deleted — removing the row from under the runner would leave `appendStep` silently no-oping while the work carried on invisibly, so the button reads "Stop this job" while live and "Delete this job" once it isn't. Deleting a job **keeps the files it produced**: they are the output, they outlive the record of how they were made, and quietly deleting a call list because someone tidied a job list would be the wrong surprise. File deletion is a separate endpoint for the same reason, still behind `safePath` (traversal refused) and 404-ing honestly on a missing file. Verified **15/15** on deletion (running job refuses with a reason, unknown id 400s, traversal refused, missing file 404s, X present on every row, live row says Stop, deleted job gone server-side with its files intact on disk, live row cancels instead, file gone from panel and from disk) and **12/12** on the chat handoff, driven against production's real job through a running → running → done sequence.

- [2026-07-29] — **Harvey can run code (Phase A: hardened local runner, DEFAULT OFF)** (`src/core/codeExec.ts` (new), `run_script` tool, `GET /api/harvey/exec-status`, `HARVEY_EXEC_MODE`, `JobStep.files`, `public/jobs.html`). Closes the last Cowork parity gap from `docs/harvey-code-execution-scope.md`. **The argument is correctness, not convenience:** a model doing arithmetic over 200 rows in its head will sometimes be wrong and always sound certain; a script turns "Harvey says $412k" into "Harvey computed $412k, and here is the script". **`HARVEY_EXEC_MODE` defaults to `off` and is off in production** — a capability this sharp does not arrive because something happened to deploy. Three modes: `off` (refuses), `local` (this), `sandbox` (Phase B — refuses honestly with "not built yet" rather than silently falling back to local). When off, `run_script` is **not even in the tool list**, so Harvey cannot promise to compute something and then discover he can't; the mode is re-checked at call time too, so a replayed conversation cannot slip through. **What `local` actually enforces, all adversarially tested:** the child environment is built by **allow-list** (PATH, HOME, TMPDIR, LANG, LC_ALL and three markers — 8 keys) so no `ANTHROPIC_API_KEY`, `BRIVITY_API_KEY`, `TWILIO_AUTH_TOKEN` or Fly token is reachable; network is stubbed at the language level (Node: `net`/`http`/`https`/`tls`/`dns`/`child_process`/`worker_threads` throw on require and global `fetch` is deleted; Python: a `sitecustomize.py` raises on socket creation, which sits under urllib/requests/http.client); Node runs under **`--permission`** with fs scoped to the run folder, so `/etc/passwd`, `../../../etc/passwd` and `/data/tracker.db` all fail `ERR_ACCESS_DENIED`; no shell; hard wall-clock timeout (20s default, 60s max); output and artefact caps. Inputs are **copied into** the run folder rather than the script being pointed at the workspace — more work per task, and the property that makes it safe. Every run writes `runs/<id>/script.js`, `output.txt` and anything produced into the workspace, so it is **downloadable and auditable through the existing endpoints** with no new storage layer. **A timeout is reported AS a timeout and a crash AS a failure**, each with an explicit `say` forbidding the tempting summary — a script killed at 20s described as "no matches found" would be the worst possible outcome because it looks like an answer. `JobStep.files` was added because one `path` cannot represent a run's several artefacts. **Honest limit, stated in the module doc, in `execStatus()` and in the UI: `local` is HARDENING, NOT ISOLATION.** The process still runs in the app's own container as root; a script cannot authenticate anywhere, but it is not provably contained. That is exactly why Phase B (a disposable Fly Machine with no secrets, no volume, no egress) is next, and why this ships off. Verified **30/30** on the runner (secrets unreachable via node and python, network refused both ways, `child_process` refused, fs escape denied in four directions, infinite loop killed at 2.0s of a 2s budget, crash surfaced with real stderr, 200k-line output truncated, off-by-default, sandbox refusing) and **11/11** on the tool boundary (absent when off, refused if called anyway, computes through the boundary, returns artefact paths, correct `say` for each outcome).

- [2026-07-29] — **Harvey Jobs reads like a conversation, not a debug log** (`public/jobs.html`; `docs/harvey-code-execution-scope.md` (new)). Operator feedback on a working job: *"it's a bit overwhelming and codey… hide all the back-end stuff, just have it be like a normal chat with the file being able to be downloaded."* He was right — raw tool JSON and a step trace sat **above** the answer, so finished work looked like output from a build. The detail pane is now a two-message transcript: the task as a message from Marco, the result as a reply from Harvey with **rendered markdown** (headings, bold, real HTML tables — every report Harvey writes is mostly tables, and they were being shown as raw pipe characters), and the files it produced as **download cards** (icon, plain filename, folder, size, Open / Download) inside the answer. The tool trace is collapsed behind one plain-language line — *"▸ Show what Harvey did (13 tool calls)"* — kept in full because it is how a number gets audited, just no longer the first thing you read. While a job runs, the current step is shown in operator language (`search_tracker` → "searching the tracker") rather than as a function name. `.md` previews render as prose; `.csv`/`.json` stay verbatim, because those files ARE their raw text and prettifying them would misrepresent them. The markdown renderer is ~60 lines, hand-rolled (the fast overlay deploy path cannot `npm install`), and escapes everything **first** before re-introducing a known set of inline tags, so a lead's note containing `<script>` renders as text. File sizes come from the workspace listing and the pane re-renders when it lands, so a card degrades to "file" rather than claiming "0 B". A job that stops being live triggers a file-list refresh, so its output appears without anyone hitting refresh. Verified 21/21 in a real browser against production's real jobs and report files (answer before trace, table with every row, no pipes on screen, body font not mono, card with correct filename/size, download byte-identical, trace collapsed with no JSON visible then fully revealed on click, `.md` preview rendered). **Also scoped, not built:** `docs/harvey-code-execution-scope.md` covers the one remaining Cowork parity gap — Harvey cannot write and run a script. Three options costed (local `child_process`, a disposable Fly sandbox machine, a hosted sandbox API); recommendation is the **Fly sandbox machine** (~1–1.5 weeks) with the tool surface built first behind `HARVEY_EXEC_MODE` defaulting to **off**, because option A's failure mode is a generated script holding every credential and the SQLite files with every client record. The doc's real argument is correctness, not convenience: a model doing arithmetic on 200 rows in its head will sometimes be wrong and always sound certain.

- [2026-07-29] — **Harvey's produced files are reachable: real downloads + a workspace browser** (`public/jobs.html`, `GET /api/harvey/workspace/download`, `src/hull/jobRunner.ts`, `src/core/jobStore.ts`). Operator report: a job wrote a call list and "it's not an actual file, it seems like an output". The file was always real — 949 bytes on the `/data` volume — but **nothing in the UI linked to it**, which for the operator is the same thing as it not existing. **Root cause:** the job step's `input` is truncated to 600 chars to keep job records small, and `filesTouched()` in the jobs page recovered the path by `JSON.parse`-ing that field. Any write big enough to be worth downloading produced invalid JSON, the parse threw, and the "Files written" section silently vanished — so it failed on precisely the jobs that had produced something. Verified against production: **all four** existing jobs had unparseable truncated input. The runner now records the path as a first-class `JobStep.file`, taken from the tool's own RESULT (`{ok:true,path:…}` — small, structured, and only present when the write succeeded) rather than from the truncated input echo; the page prefers that field, falls back to a regex over the step OUTPUT so **jobs written before this fix also resolve**, and only then to the input. New `GET /api/harvey/workspace/download` serves raw bytes with a per-extension content type and `Content-Disposition: attachment` (`?inline=1` opens in-browser instead) — the pre-existing JSON reader is right for a preview and useless when someone wants the file. `safePath` still does containment, so traversal is refused (`path escapes the workspace`). The jobs page gained view / download / open per produced file, plus a **Workspace files** panel listing everything with sizes — needed because Harvey can also write files straight from chat, and those were unreachable from every screen. Verified 12/12 in a real browser against the real route and real files on disk (download fires with the correct filename and byte-identical content, csv gets `text/csv`, preview still works, all workspace files listed) plus direct route checks for the inline variant, traversal refusal and a missing file.

- [2026-07-29] — **Monthly reminder to import the Brivity transaction export** (`src/agents/transactionImportReminder/` (new), `POST /api/transactions/import-reminder/run`, scheduled at boot in `src/server.ts`). Brivity has no transaction API, so the transaction board is only ever as fresh as the last CSV somebody uploaded — and left alone it rots silently. **It is staleness-driven, not calendar-driven:** the reminder is raised when the last import is over 30 days old (or has never happened), so importing on the 3rd means nothing nags on the 1st. A reminder that fires whether or not the work was already done is one the team learns to dismiss, and then it fails for the case that matters. **It clears itself** — a successful import closes any open reminder synchronously inside the import request, not on the next tick, because being told to do something you just did is exactly how a reminder trains people to ignore it. Exactly one open reminder exists at a time (matched by title, so it survives restarts); at 45 days it is moved to Urgent once and then left alone rather than duplicated. **The task is written to the COMMAND task store (`core/db.ts`), which is what `/team-tasks` renders — NOT the separate CRM task store behind `/api/crm-tasks`.** That distinction was the one real trap here: writing to the CRM store compiles, stores and reads back fine, and produces a reminder nobody ever sees. The check runs at boot and every 12h rather than monthly — the check is cheap and idempotent, and a once-a-month timer would be skipped outright by any deploy landing on the wrong day. Verified 25/25 on the lifecycle (boot raises it, repeat runs never duplicate, an import clears it, 29 days quiet / 31 days raises, escalation happens once, a later import clears the escalated one) plus 4/4 that it actually renders on the Tasks board and 5/5 that the escalation is visible as a real red border rather than only a class name.

- [2026-07-29] — **Brivity transaction sync: CSV import path + Transaction Snapshot** (`src/core/transactionImport.ts` (new), `src/core/transactionsStore.ts`, `POST /api/transactions/import-csv`, `GET /api/transactions/import-status`, `public/crm-brivity.html`, `public/tracker.html`, `docs/brivity-transaction-api-request.md` (new)). **The finding that shaped this: Brivity's Core API does not expose transactions.** `api.brivity.com/api/people` is the only route that reaches authentication (401 `Bad Credentials` without a key); `/api/transactions`, `/api/deals`, `/api/listings`, `/api/escrows`, `/api/closings` and the `/api/v2/*` equivalents all return a generic `406 Not Acceptable` — **and so does a deliberately nonsense path**, which is what proves 406 is their catch-all rather than a real endpoint. So there is nothing to poll, and a "real-time synced from the CRM" widget cannot be built today. Transaction data arrives by report export instead. **Three things were wrong before this change:** (1) the CRM's `⭱ Import CSV` button parsed and mapped client-side and POSTed to `/api/deals` → `deals.json`, while the Snapshot, deadline and document engines read `/api/transactions` → `transactions.db` — so an import could appear to succeed and leave the board empty; (2) the CRM's transaction list read `body.deals` from the dashboard snapshot, not the real store; (3) `loadLive()` bails out early when fewer than three leads come back, which would have hidden genuinely-imported transactions behind the demo set. Parsing and column mapping now live server-side in one place (`transactionImport.ts`, dependency-free — the overlay deploy path cannot `npm install`), the page hands the file over and re-reads the store, transactions load **before** the sparse-leads bail-out, and `deals.json` is consulted only when the real store is empty. The import **dry-runs first** (same shape as the Brivity people import), dedupes on MLS # then address so re-importing the same export updates rather than duplicates, reports columns it could not place instead of dropping them silently, and preserves Brivity's original status string in `parties.legacyStatus` because our six-state lifecycle cannot represent "coming soon" or "referral". A re-import refreshes only the facts that came from Brivity — inspection/final-week/post-close flows, deadlines and documents a human built on top are untouched. New `transaction_imports` table + `imported_at` on every row: **the CRM header and the Snapshot both state their as-of date and say plainly that this is not a live feed**, and with an empty store the Snapshot renders no tiles at all and names the reason rather than a wall of $0 — same rule as the analytics roll-up, a zero must never stand in for "no data". The **Transaction Snapshot** card now sits beside the two funnels on the tracker's Funnel view (under contract / active / closed YTD / GCI), completing the requested layout. `docs/brivity-transaction-api-request.md` is a ready-to-send request to Brivity for transaction API or webhook access — the only path that would earn the words "real-time"; when it lands, the swap is a `brivityTransactions.ts` modelled on `brivityPeople.ts` reusing the existing mapping. Verified in a real browser: 16/16 on the import (idempotent re-import, Brivity statuses surviving the round trip, closed deals valued at close price not list price, GCI parsed from `$12,555`, cancelled deals excluded from active and closed), 6/6 on the zero state, and the prior suites re-run green (43/43, 11/11).

- [2026-07-29] — **Tracker gains a Funnel view; Tracker moves into the CRM and the DM Agent into Analytics** (`public/tracker.html`, `public/crm-brivity.html`, `public/analytics.html`, `public/shell.html`). **Navigation:** the shell had Tracker and DM Agent as top-level items alongside CRM and Analytics, so the same book of people lived in two places and the team hunted for a contact in whichever one they opened first. Tracker is now a rail view inside the CRM (between Opportunities and Transactions) and the DM Agent a tab inside Analytics. Both are **embedded, not reimplemented** — each is a complete page with its own state, and a second copy would drift within a week; `/tracker` and `/chat` still serve standalone. Both iframes are **lazy**: the src is set on first open, so visiting an unrelated CRM view no longer pulls 1,219 records, and once loaded the frame stays mounted so switching tabs doesn't discard an open conversation. The tracker hides its own logo when embedded. **Funnel view** (fourth option beside Both): both pipelines as ordered stages with drop-off. "Reached" is **cumulative** — someone Under Contract has necessarily been Contacted — because per-stage occupancy would draw a sawtooth implying people had moved backwards. Empty stages are shown at zero rather than hidden, and the footer states how many are unstaged plus a plain warning when only one stage is in use, since a funnel drawn over the three stages production actually uses would look like a healthy pipeline if the empty ones were dropped. Today that is buyers 228 Contacted of 865 (637 unstaged) and sellers 354 New of 354. **Honest gap on the requested screenshot:** it also showed a "Transaction Snapshot — real-time synced transaction data from the CRM" card. That was **not built.** `/api/transactions` is empty and there is no Brivity transaction sync — transactions only exist when someone creates them by hand — so the widget would be a heading over permanent zeros. It needs a transaction sync first. **Two real write-path bugs found and fixed while testing:** (1) bulk-staging from the **Both** view sent `side:"both"`, and the server defaults anything that isn't "seller" to **buyer** — so moving a seller quietly wrote their *buyer* stage. Merged stage options now carry which pipeline(s) they belong to and the selection is split into one request per side; a seller-only stage no longer drags selected buyers along. (2) **+ Add Person** in the Both view created records with `sides:["both"]`, i.e. someone who appears in neither column; it now asks which pipeline. Every side test in the page routes through one `sidesShown()` helper so a future view cannot silently filter for a side no record has. Verified in a real browser against production's 1,219 records: **43/43** on the three features (funnel counts checked against an independently computed cumulative funnel, lazy-load, no duplicate frames, shell nav) plus **11/11** asserting the exact bulk-stage request bodies.

- [2026-07-28] — **Per-device browser pairing with Marco first; TikTok staleness explained** (`src/core/browserControl.ts`, `public/extension/*` → manifest `1.5.0`, `src/core/socialAnalytics.ts`, `public/analytics.html`, new `BROWSER_DEVICE_PRIORITY`). **Devices:** there was one global queue, so with two browsers armed a command went to whichever polled first — at random, and nobody could tell which machine had acted. Each paired browser now has a generated id and a name typed in the popup; every command is addressed to a device **before** it is queued, and a poll drains only its own commands. Unaddressed commands go to the highest-priority armed browser — **Marco's**, per `BROWSER_DEVICE_PRIORITY` (default `marco`, substring match so "Marco's MacBook" counts). Naming a device that is present but switched OFF is an **error**, never a silent fall-through to someone else's browser. Disarm with no device named stops **every** browser (that is what "stop" means out loud); arm targets exactly one, because arming every paired machine at once would arm a browser nobody is looking at. **Two real bugs found while testing this:** (1) a closed browser kept winning the routing tiebreak on "first seen", so commands went into the void while a live browser sat idle — same-priority devices are now ordered by most-recently-heard-from, which a closed browser loses within seconds, while priority stays the primary key so Marco vs Wesley never flip-flops; (2) an offline browser was still reported as **armed**, because its last poll said so — a closed laptop showing as controllable is the same class of lie as Harvey claiming he had switched the browser off, so `enabled` is now false unless the device is currently connected, and devices unheard-from for ten minutes are forgotten entirely. **Known limit, inherent to polling:** a browser that closes stays routable for up to ~30s (one long-poll window plus slack); the command then fails with a clear timeout rather than silently. **TikTok staleness:** an earlier claim of mine — "nobody has run a refresh" — was **wrong**. The nightly pull is scheduled, starts at boot, and has been running and FAILING every night since 15 July with Apify's `Monthly usage hard limit exceeded`; the pull log shows it firing tonight at 23:00 and erroring. Nothing surfaced that, so Analytics said "stale — refresh in Content Manager" and pressing Refresh could only fail the same way. The TikTok tab now reports the actual failure and its timestamp, and says plainly that Refresh will hit the same error. **This is a quota/billing issue on the Apify account, not a code defect** — no amount of scheduling fixes it. Verified 68 checks across six browser suites (11 device-routing, 29 capability, 10 window, 10 screenshot, 3 dead-site, 5 worker-kill), including two browsers armed simultaneously with commands proven to land on Marco's and never Wesley's.

- [2026-07-28] — **5. Analytics: per-platform tabs + a combined roll-up** (`public/analytics.html` (new), `src/core/socialAnalytics.ts` (new), `src/integrations/zernio/` (new), `GET /api/analytics/social`, `/analytics`, new **Analytics** item in the shell nav under Growth). One tab per platform with that platform's metrics broken out, plus an "All platforms" tab rolling them up, per the spec. **TikTok is genuinely live** off the existing Apify pull via `socialStore` — 100 posts / 848k views / 43.7k likes on production today — and it is currently the *only* real source; Instagram, Facebook and YouTube arrive when Zernio is connected. **The design rule this subsystem exists to enforce: a platform with no data source must never be indistinguishable from a platform with zero engagement.** Every metric is `number | null` (null = "we don't know", 0 = "measured, and it's zero"), every platform carries a `status` (`live` / `not_connected` / `error` / `unreadable`) and a named `source`, and the combined tab **sums only platforms that actually reported and names the ones it left out** — a total that silently omits Instagram is a lie by rounding, so the UI states its coverage before showing any number. Staleness is surfaced too, not hidden: the production TikTok pull is 13 days old and the tab says so rather than presenting it as current. `src/integrations/zernio/` ships as a real client that is **inert without `ZERNIO_API_KEY`** — unconfigured it returns no numbers at all rather than plausible placeholders, because a fabricated baseline poisons exactly the decision the page exists to support. **Honest limit on that module:** the transport (auth, timeout, error handling, normalisation) is written and tested, but the response FIELD NAMES are not verified — there was no key or API doc available — so `mapZernioPayload` reads a set of common spellings and, when it recognises nothing, reports `unreadable` instead of returning zeros; finishing the wiring means setting the key and correcting the field names in that one function. Verified by seeding the local store with production's real numbers and driving the page in a real browser: tabs render, TikTok shows live figures with a stale-data banner, the combined tab names its coverage and excludes the three unconnected platforms, and the Instagram tab renders **no digits whatsoever**. **Harvey reads the same data** via `get_social_analytics` (`platform` optional, defaults to the roll-up), so "how did TikTok do this month" doesn't need the page open. The same partial-data discipline is enforced at the tool boundary rather than left to the model's judgement: an unconnected platform returns `hasData:false` with **no numeric fields at all** — so there is literally no zero available to misreport — plus an explicit `say` instruction not to imply the account is underperforming; the combined result carries `missing` and a `say` requiring him to name what the totals exclude; and staleness travels with the numbers. 12/12 checks, including that a not-connected platform's payload contains no numbers and that an unknown platform is refused with the valid list.

- [2026-07-28] — **Harvey opens sites in his own WINDOW and shows them; voice stops dying when the shell is backgrounded** (`public/extension/background.js` → manifest `1.4.0`, `public/aethon-voice.js`, `browser_navigate` gains `focus`). Two operator reports with one shared cause. (1) *"When he opens a website it doesn't take you there."* Navigation opened a background tab, so the operator had to hunt for it — `navigate` now focuses by default (`focus:false` opts out for a step they didn't ask to watch). (2) *"He stops listening after a while."* Simply activating Harvey's tab in the same window would have made this worse: it would hide the Harvey shell, and **browsers throttle timers in hidden tabs to roughly one per minute** — including the STT socket's `setTimeout` reconnect, which is the only recovery path. A two-second reconnect could therefore sit dead for a minute or more, which is exactly the reported symptom. So Harvey's page now opens in a **separate window**, leaving the shell the active tab of its own window (visible, unthrottled, mic alive) while the site still comes to the front. Voice was hardened to match, because occlusion can still hide the shell: a health check reconnects a socket found closed, `visibilitychange` / `focus` / `online` trigger an **immediate** check (events aren't throttled, so returning to Harvey is instant rather than dead for a minute), a suspended mic `AudioContext` is resumed (it yields silence rather than an error, so the socket looks healthy while nothing is heard), and reconnects use capped backoff instead of hammering a dead endpoint every 2s. **Also fixed, found while testing:** `navigate` returned `ok:true` against a page that never loaded — Chrome reports its own error page as a completed load — so Harvey would describe a site he never reached. It now probes the document after loading and fails honestly, and separately reports "loaded, but the thing you were waiting for never appeared" instead of a bare success. Verified 10/10 window behaviour (separate window, shell never navigated and still the active tab of its window, second navigate reuses the window, `focus:false` honoured) and 3/3 on the false-success fix, with the full suite re-run green (29/29 + 10/10 screenshot + 5/5 worker-kill). **Not verifiable in this sandbox:** real front-most window ordering — there is no window manager, so Chrome reports every window as focused; confirmed by calling `chrome.windows.update({focused:true})` directly and seeing no change. The live voice drop could not be reproduced either (no microphone or ElevenLabs key here), so the voice fixes address the mechanism, not an observed failure.

- [2026-07-28] — **Harvey can SEE a page, not just read it** (`browser_screenshot` tool, `captureTab()` in the extension → manifest `1.3.0`, `toolResultContent()` in `src/hull/agentLoop.ts`). Closes the one capability deliberately deferred from the hardening pass. Some pages only make sense visually — a map of comps, a scanned disclosure, a floor plan, a price baked into an image — and no amount of DOM reading recovers that. **The real work was the vision plumbing, not the capture:** tool results were flattened to a string before reaching the model, so an image could only ever have arrived as a base64 blob it could describe but not look at. Tools may now return `_image: {media_type, data}`, and `toolResultContent()` lifts it into a genuine Anthropic image block alongside the text. `serializeToolResult()` **strips `_image` everywhere a result is flattened**, so a few hundred KB of base64 can never leak into a log line, the job runner, or the audit trail. Two constraints shaped the extension side: `captureVisibleTab` only ever photographs the ACTIVE tab, but Harvey's work tab is deliberately backgrounded — so it is brought forward for the capture and the operator's tab restored in a `finally`, even if the capture throws (leaving someone staring at Harvey's tab is worse than failing the command); and a raw 1920px PNG is megabytes, so it is re-encoded through `OffscreenCanvas` to ~1000px JPEG, with a fallback to Chrome's own JPEG if `OffscreenCanvas` is unavailable and chunked base64 because spreading ~200k bytes into `String.fromCharCode` overflows the argument stack. Screenshots are excluded from the 200-entry audit history — the fact of a capture is recorded, not the pixels — and `/api/browser/result` went 1mb → 6mb since the image rides back in that body. Measured **13 KB, ~400ms** on the test portal. Verified 10/10 end to end: capture succeeds, payload decodes to a structurally valid JPEG, small enough to send, operator's tab handed back, shell never navigated, the agent loop builds a real `text+image` block, and base64 is absent from the string form. Full suite re-run green afterwards (29/29 + 5/5).

- [2026-07-28] — **Browser control hardening pass: reliability, latency, and pages that actually fight back** (`public/extension/*` → manifest `1.2.0`, `src/core/browserControl.ts`, `src/harvey/platformTools.ts` + 5 new tools). Driven by a live voice session against a test portal. **Reliability — the big one:** the poll loop lived in a `setTimeout` chain inside an MV3 service worker, and Chrome terminates those after 30s idle, taking every pending timer with it. Polling stopped silently: the extension still looked paired and simply never acted again, which is the most likely explanation for "it took a while to pull up the site". Now a `chrome.alarms` watchdog (30s, Chrome's floor) survives termination and restarts the loop, and a keepalive ticks an extension API every 15s so a parked request or a slow page can't get the worker killed mid-command. Verified by terminating the worker for real over CDP and watching it come back and resume working. **Latency:** polling was a fixed 2s tick, so every step of a multi-step task paid up to 2s of dead time before starting. The poll now parks server-side until a command exists (20s cap, under the worker kill) — measured dispatch **~11-14ms**. **A command-loss bug found while testing this:** a parked poll from a browser that had gone away still won the race in `wakeWaiters()`, drained the queue and resolved into a dead socket, so the command vanished and the caller was told "the browser didn't respond" about a browser sitting right there. A newer poll now retires older ones before draining. **Page handling** (each verified against a fake portal built to reproduce the real failures): queries pierce open shadow roots and same-origin iframes, so web-component and iframe content stops looking like "the site changed"; `waitFor` plus SPA-aware navigation fixes reads that returned a spinner because `status:"complete"` fires long before a listing renders; `structured` reads schema.org JSON-LD / OpenGraph, which is how listing portals actually publish price/beds/baths and survives redesigns that break every selector; `scroll` pulls in lazy-loaded results; fill now handles `<select>` (by option value **or** visible text), checkboxes, radios and contenteditable; click scores candidates so an exact label beats a longer one that merely contains the phrase. **Remote arm added at the operator's request** — `browser_enable` alongside `browser_disable`. This one escalates capability, so it is bounded: it only works on an already-paired browser, and the popup carries a **"Let Harvey switch it back on"** lock that refuses it outright; the honest trade (anyone with the pairing token can re-arm a paired browser unless that lock is set) is stated in the popup rather than hidden. **Password handling clarified, not changed:** Harvey was telling the operator the refusal was "browser security", sending them hunting for a Chrome setting that does not exist. The refusal reason and tool description now say plainly that it is Harvey's own rule, and point at the real answer — his tab runs in the operator's own Chrome profile, so **he never needs a password**; at a sign-in wall `browser_show_tab` brings the tab forward and the human signs in, session intact. `read` now reports `needsLogin` so a login wall stops being reported as an empty listing. Verified: **29/29 end-to-end checks, three consecutive runs**, plus **5/5** service-worker-termination checks.

- [2026-07-28] — **Browser control: a real off switch, bare-domain URLs, and no pre-emptive refusals** (`src/core/browserControl.ts`, `src/harvey/platformTools.ts`, `public/extension/background.js`, new `browser_disable` tool, manifest `1.1.0` → `1.1.1`). Found by watching a live voice session against a test site. **SECURITY-RELEVANT HONESTY BUG, now fixed:** asked to turn browser control off, Harvey answered *"Harvey browser control is now off"* and *"Already off"* — he had no such capability, the toggle existed only in the extension popup, and the browser stayed armed. Claiming a safety control is engaged when it isn't is worse than refusing. The server can now request a disarm, the extension honours it on its next poll (~2s) and any queued commands are dropped so "off" means off immediately; `browser_disable` is wired to it and its description forbids claiming the browser is off unless the tool succeeded. **The remote path is deliberately one-way — disarm only, never arm.** Arming stays a physical act by the person at the keyboard, otherwise anyone holding the pairing token could switch on a signed-in browser. (2) `browser_navigate` now normalizes bare domains — `books.toscrape.com` → `https://books.toscrape.com/` — after Harvey demanded the operator retype an address with the scheme four times in a row; non-addresses ("find me a listing in dallas", a hostname with no dot) are still refused rather than turned into a bogus navigation. (3) `browser_fill` told Harvey "password fields are refused by design", which he read as *don't try* — he declined whole forms that merely contained a password box. The description now requires him to run the fill and report which fields were filled vs refused; the page-side refusal was always per-field and unchanged. Verified in real Chrome against the real bus: 9/9 disarm checks (armed → acts → disarm → extension reports off → popup checkbox unticked → commands refused → second disarm honest → server cannot re-arm → human can), 11/11 URL-normalization cases, and 4/4 work-tab regression checks after the poll response changed shape from a bare array to `{commands, disarm}`.

- [2026-07-28] — **Harvey drives his own tab instead of the one you're reading** (`public/extension/background.js`, popup + README wording, manifest `1.0.0` → `1.1.0`). **Fixes a self-destruct bug**: every command targeted `chrome.tabs.query({active:true})`, and the active tab is normally the tab the Harvey shell is open in — so "pull up that listing" ran `chrome.tabs.update()` on the shell itself, navigated it to Zillow, and killed the live conversation mid-task. The extension now keeps a **dedicated work tab**, created in the background (`active:false`) on the first `navigate` so focus stays on the shell, reused by every later command, and reopened automatically if the user closes it. The tab id lives in `chrome.storage.local` (**not** a module variable — MV3 service workers get killed and would forget it, stranding Harvey on a tab he could no longer find). **The shell is excluded structurally, not by luck of focus:** any tab whose origin matches the paired server URL is refused as a work-tab target and disowned if the work tab drifts onto it, so no amount of tab-switching can make Harvey navigate the app out from under the operator. Non-navigate commands with no work tab yet **adopt the user's current tab** — but only when it is a real site tab, never the shell and never a `chrome://` page — which keeps "what does this page say about the HOA?" working without an explicit open first. `wait` no longer requires a tab at all. Poll/result reporting switched from the active tab to the work tab, so `/api/browser/status` and the popup show the page Harvey is actually acting on rather than whatever the human just clicked over to (popup now reads "Harvey's tab:"). Verified with 28 scenario checks driving the real `background.js` against stubbed `chrome.*` APIs: shell never navigated, one tab reused across repeated navigates, recovery after the user closes it, adoption allowed for a site tab and refused for the shell and for `chrome://extensions`, work-tab disown on origin drift, click/fill hitting the work tab while the shell is focused, and the `onRemoved` cleanup. Served zip re-checked (`unzip -t` clean, `diff -r` byte-identical, `manifest.json` still top level).

- [2026-07-28] — **Extension is installable without a git clone** (`extension/` → `public/extension/`, `src/core/zipWriter.ts` (new), `GET /api/browser/extension.zip`). Chrome's "Load unpacked" needs a real folder on the operator's own machine, and the extension source only ever existed in the repo — so installing it meant cloning, which is not a reasonable first step for the person who has to run it. The server now serves its own copy as a zip. Two structural notes for anyone touching this: (1) the folder **moved under `public/`** because neither the Dockerfile nor the fast overlay deploy copies a bare `extension/` at the repo root — a root-level folder would have been absent from the running image, so the endpoint would 500 in production while passing locally; `public/` is copied by both paths. (2) `zipWriter.ts` is a hand-rolled ~90-line ZIP writer on `zlib.deflateRawSync` rather than `archiver`/`adm-zip`, because the overlay deploy path cannot run `npm install` and a new dependency would force a full image rebuild. Entries are stored **flat, with no wrapping folder**, so Windows "Extract All" and macOS Archive Utility both produce a single folder with `manifest.json` directly inside — the shape Load unpacked expects. The route is deliberately **ungated**: it carries no secrets, the pairing token is typed in afterwards, and gating it would only lock out the person installing it. Verified end to end — served zip passes `unzip -t`, extracts byte-identical to the source (`diff -r`), and the extracted folder loads into a real Chromium with its service worker running and the popup rendering. Shipped alongside it: the popup used to render **every** pairing failure as "Can't reach the server", including a 401 where the server answered fine and the token was the problem — it now names which of the four it is (no token set server-side / token mismatch / wrong address / genuinely unreachable), and normalizes whatever is pasted into **Server** down to its origin, because people paste the page they're on (`.../shell`) and every API call then 404s with nothing on screen explaining why.

- [2026-07-28] — **1.2 Browser extension: Harvey can drive a real browser** (`extension/` (new, MV3), `src/core/browserControl.ts` (new), `/api/browser/poll|result|status`, 6 Harvey tools). Closes the last open item from section 1. Harvey can now navigate, click, fill forms and pull data from sites with **no API** — a listing portal, a title company's form, an MLS back office — by driving the operator's own Chrome tab. Tools: `browser_status`, `browser_navigate`, `browser_read`, `browser_click` (by visible label, not just selector — that's how a person describes a button), `browser_fill`, `browser_extract` (name→selector map, so a listing lifts into structured data in one call rather than parsing prose). **Transport is polling, not a socket**: the server cannot reach into a browser, and polling survives sleeping laptops, dropped wifi and Fly restarts with no reconnect logic and no new dependency. **SECURITY — this is the most dangerous surface in the app**, executing actions in a signed-in human's browser with their cookies. Three controls, none optional: (1) a pairing token, and `tokenMatches()` **fails closed** — an unset `BROWSER_CONTROL_TOKEN` disables control entirely rather than allowing everyone, deliberately unlike `DASHBOARD_TOKEN` elsewhere; (2) the extension is **off by default** and armed per session by the human — switched off it still polls (so the UI can show it's reachable) but is handed nothing; (3) every command is recorded with its result for audit. **Deliberately not offered:** reading cookies or localStorage, typing into password fields, or executing arbitrary JS — those turn "fill in this form" into "exfiltrate this session" and no use case needs them; password fields are refused in the page-side fill and the refusal is reported back. Verified by loading the real unpacked extension into a real Chrome via Playwright and driving a fake no-API listing portal end-to-end: 17 checks covering pairing through the actual popup UI, refusal while switched off, read/extract/navigate/fill/click, the password refusal (asserted the box stayed empty), the audit trail, and 401s on a bad token. A test-only `/__test/tool` HTTP hook exists for this, registered **only** when `BROWSER_CONTROL_TEST_HOOK=1` — never set in production, so there is no route at all otherwise. Install steps are in `extension/README.md`, and a Knowledge Center doc covers it so Harvey can explain it.

- [2026-07-28] — **CRM write tools, 3.4 Knowledge Center, and suggestions without a linked lead** (`src/core/knowledgeStore.ts` + `public/knowledge.html` (new), `/api/knowledge*`, 7 new Harvey tools, `/api/suggest-reply` + `/api/complete-reply`). **CRM write tools** finish the Harvey+CRM pairing: `get_lead`, `update_lead` (status, stage, intent, priority, name, phone, email, notes), `log_lead_activity` (call/text/email/note onto the timeline) and `create_lead`. `update_lead` only writes fields actually passed — verified that "mark her hot" leaves phone, email, notes, criteria and tags untouched, which is the dangerous case. Invalid enum values are refused with the valid list rather than coerced; `create_lead` refuses a contact with no phone AND no email, and dedupes against existing records by phone (any format) or email (case-insensitive), returning the existing id instead of splitting someone's history. Activity writes go through the existing `appendLeadActivity` so normalisation isn't bypassed. **3.4 Knowledge Center** replaces the How to Use tour (still at `/how-to`): a markdown SOP library on the `/data` volume, with categories, search, and a full editor. The point is onboarding — the same documents back the page *and* Harvey's `search_knowledge` / `read_knowledge_doc` / `list_knowledge`, so pointing a new assistant at Harvey and at the page can never give different answers. Seeded with 7 starter docs describing **the software**; these are explicitly **not** Marco's business SOPs — nobody but the team can write those, and inventing them would be worse than an empty shelf. Search is keyword scoring with a **relevance gate**: an early version returned 5 hits for "commission split for referral partners" (undocumented), which would have had Harvey confidently citing an unrelated SOP; it now requires ~60% of the query's meaningful words plus at least one title/tag/category hit, and stopwords are stripped. Verified 7/7 on find-vs-refuse cases, and Harvey's tool returns an explicit "not documented, do not invent a process" note on a miss. **Suggestion fix:** the composer refused to draft when a conversation had no `leadId` (group threads, and the demo set the CRM keeps while fewer than 3 live leads have loaded). New lead-free endpoints draft from the thread itself and report `stage: "unlinked"` so the panel never implies it read a record it didn't have.

- [2026-07-28] — **3.3 AI suggested replies, scheduled-queue viewer, manual send time** (`src/core/replySuggest.ts` (new), `POST /api/leads/:id/suggest-reply` + `/complete-reply`, `public/crm-brivity.html`). **3.3:** a spark button in the composer drafts the next reply, and inline ghost text completes the sentence being typed (Tab or → accepts, Esc dismisses). Both read the contact's **whole record** via one context builder — every message in the thread (last 40), plus criteria, intent, pipeline stage, status, source, property inquired about, campaign, tags, CRM notes and recent activity — because the last message alone is meaningless ("yes that works" needs the six before it). Verified with a 15-field coverage assertion over a fully-populated lead. The panel shows the draft, a one-line *why*, and what it was **based on** ("Read 5 messages, their criteria, your notes, stage: qualified") so it isn't a black box, with Use / Edit / Ignore. **Suggestion only — neither endpoint sends anything**; the draft lands in the composer and a human decides, which matters because this text goes to real clients. The prompt is grounded ("use ONLY facts in the record; never invent an address, price or appointment"). A failed draft surfaces the real error rather than an empty panel, since "Harvey had nothing to say" and "the API key is unset" are very different problems; ghost-text failures stay silent, as nobody asked for that prediction. **Queue viewer:** a SCHEDULED tab in the Message Center lists queued messages with status, channel, send time, a "queued by Harvey" marker, per-row Reschedule/Cancel, and a banner when a channel can't deliver — scheduling had been write-only from the UI. Its pending count loads on page load, so the tab shows depth without opening it. **Manual send time:** the schedule popover now has explicit date + time inputs alongside the quick picks and free text; all three resolve through the same server parser and preview, and setting one clears the others so they can never disagree. Verified with 14 browser checks.

- [2026-07-28] — **3.2 Scheduled sending + Harvey's first CRM write tools** (`src/core/scheduledMessages.ts`, `scheduleTime.ts`, `scheduledSender.ts` (all new), `/api/scheduled*`, 3 Harvey tools, send-later in `public/crm-brivity.html`). Texts and emails can now be queued for later two ways, both landing in one queue and one sender: the user picks a time in the CRM composer, or **Harvey queues it from plain language** ("send this Tuesday morning"). `scheduleTime.ts` resolves phrases against the **team command time zone** (the same zone the task board uses), handling "tomorrow at 8am", "in 2 hours", weekday + daypart, bare clock times and explicit dates — and **returns null rather than guessing** on anything ambiguous, so Harvey asks instead of inventing a time; backward-looking phrases ("at 4pm last tuesday") are refused outright after that case was found scheduling a week late. Zone math is done with `Intl` (no new dependency — the overlay deploy path can't `npm install`) and is DST-correct. The composer **echoes the parsed time before you commit** via a dry-run `/api/scheduled/preview`, because discovering the interpretation by having a client get a 3am text is not an acceptable feedback loop. Queue is JSON on the `/data` volume so a mid-flight deploy loses nothing; the sender ticks every 60s, retries 3× then sticks as `failed` with the provider's real error. **Delivery honesty is enforced end-to-end: Twilio is unconfigured and the Gmail token is dead, so `canSendOn()` reports that up front and every surface — API response, composer echo, toast, and Harvey's tool result — says "queued, but it will NOT be delivered" rather than implying success.** Nothing is ever marked sent that wasn't. Verified with 21 parser cases (incl. DST and refusals), a full queue-lifecycle test against the real Twilio integration (only-due attempted, real error recorded, retry-to-failed, no early or double send), 12 browser checks on the composer, and Harvey's three tools driven directly.

- [2026-07-28] — **3.1 Message Center: channels split** (`src/core/messageChannels.ts` (new), `channel` on `DashboardLeadRow`, `public/crm-brivity.html`). The Message Center was one undifferentiated list, and in production **every one of its 228 threads is a social DM** (151 TikTok, 77 Instagram) — so a live DM and a text looked identical despite completely different urgency. Added channel sub-tabs (ALL / Instagram / TikTok / SMS / Email / Group) with **per-channel unread counts** in the tab strip, a channel tag on each row in the ALL view (hidden inside a single-channel tab, where it would just repeat the tab name), and channel-specific empty states so a quiet SMS tab reads as "nothing yet" rather than broken; channels with no threads at all are dimmed. Classification lives **server-side** in `messageChannels.ts` and is surfaced as `DashboardLeadRow.channel`, so the tabs, the counts and anything Harvey says about "unread Instagram DMs" share one definition instead of drifting. **Channel is derived from `platform` only — `source` is deliberately ignored**: 86 Brivity contacts carry a social `source` but have no social thread, and inferring from source would file them under Instagram/TikTok and mis-file them again the moment Marco texts one (that thread is SMS). Origin is not channel. Verified by running the real classifier over all 742 production rows (151 tiktok / 77 instagram / 514 other, and the 228 with threads match the platform split exactly) plus 15 browser checks on seeded multi-channel data covering tab isolation, per-channel unread counts summing to ALL, and row tagging.

- [2026-07-28] — **Sign-in is now per browser session, not remembered forever** (`public/team-session.js` (new), `public/shell.html`, `public/who.html`, `public/team-tasks.html`). The `/who` screen shipped earlier the same day only appeared when `marcoTaskUser` was *unset*, so once anyone picked a name that device never saw the login again — indistinguishable from the screen being broken (which is exactly how it was reported), and wrong for a shared machine, where the next person to sit down was silently signed in as whoever used it last. Identity and *being signed in* are now two separate things, both in the new shared `team-session.js`: `localStorage.marcoTaskUser` still holds **who you are** (unchanged contract — every page reads it directly), while a new **session cookie `mp_team_session`** written with no max-age holds **whether this browser session has signed in**. `isSignedIn()` requires both, and that they agree, so a stale name from a previous visit no longer grants entry. Result: the login screen appears each time you come to the site, then stays out of the way across tabs and sub-pages for that session. A third long-lived cookie `mp_team` only remembers the last pick so `/who` can mark it "Current" — one tap for the normal case; it never grants access. `shell.html` runs the check in a **blocking `<head>` script** so the app cannot paint for an unidentified visitor, and `/team-tasks` opened standalone does the same. Added a real **Sign out** in the sidebar footer (ends the session and returns to `/who`), alongside the existing chip which switches user. Verified with 27 browser checks across two suites, including the reported case (saved name + fresh session ⇒ still prompts), no re-prompt within a session on either the shell or a sub-page, a new session prompting again, one person handing the machine to another, and sign-out clearing identity.

- [2026-07-28] — **Team sign-in is now a real login screen** (`public/who.html` (new), route `/who`, `public/shell.html`, `public/team-tasks.html`). The "who's on" picker was a modal inside Task Command, and this morning's work had turned it into a modal inside the shell; it is now a full page at `/who` deliberately built on the same card, brand mark and layout as `login.html` (the email + password screen) so it reads as *the* login page — except the whole form is replaced by one tap on your own name. Picking a name writes `marcoTaskUser`, the single identity key every page reads, plus an `mp_team` cookie so the server can see who is on; you are then signed in across the shell, Task Command, the CRM and the rest at once, with **no per-page account switching** — which was the point. Any page opened with no identity redirects to `/who?next=<where-you-were-going>` and returns you there after picking (only same-origin `next` values are honoured, so it can't be used to bounce someone off-site). The shell's footer chip and Task Command's old in-page picker both now route to this one screen, so there is never a second place to choose a name; the in-iframe picker survives only as a safety net for the case where the shell hasn't already redirected. Switching user clears that device's popup-dedup sets so the new person's notifications count as new. **This identifies you; it is not a security boundary** — real password auth is still the separate `/login` behind `SITE_LOGIN_ENABLED`, and `/who` is ungated on purpose since gating the sign-in screen behind the thing it grants would lock the team out. Verified with 20 browser checks (redirect in, pick, land back, cookie + localStorage set, badge appears, no second prompt in the iframe or on a standalone sub-page, return visit skips the screen, switch-user round trip, `next=` preserved).

- [2026-07-28] — **Shell login gate, Tasks unread badge, and assignment emails** (`src/core/teamRoster.ts` + `src/core/taskAssignmentEmail.ts` (both new), `GET /api/team/roster`, `POST/PATCH /api/tasks`, `public/shell.html`). Three connected pieces so an assignment reaches someone who isn't looking at the app. **(1) Identity is now asked for at the shell**, not buried in the Task Command page: `public/shell.html` shows a "Who's logging in?" gate whenever `localStorage.marcoTaskUser` is unset, writing the same key `team-tasks.html` already reads — so picking a name signs you in everywhere, and the sidebar footer becomes a chip showing who you are that reopens the gate to switch. The gate renders synchronously off localStorage (before the auth/roster fetches resolve) so the app never flashes usable to an unidentified user, and falls back to a hardcoded roster if `/api/team/roster` fails — it must never lock someone out. Switching users clears the device's popup-dedup set and reloads the Tasks iframe, which reads identity once at startup. **(2) The Tasks sidebar tab carries a red count** of unread `assignment` notifications, so logging in with work waiting shows "2" without opening anything; it clears when those notifications are read in the Task Command bell (server `readAt` stays the single source of truth). Shell popups are capped at 3 per poll now that a login can surface a backlog — the badge carries the full count. **(3) Assignments also send an email** from Marco's OAuth-linked Gmail (the same sender the email-marketing agents use) naming who assigned what, with due date, urgency and a deep link to `/shell?tab=tasks`. Wired into both `POST /api/tasks` and the reassignment branch of `PATCH /api/tasks/:id`, fire-and-forget (`void`) so a slow or down Gmail can never delay or fail task creation. **The team roster moved server-side** into `teamRoster.ts` (it previously existed only as a `TEAM` array in `team-tasks.html`) because three surfaces now need it; `GET /api/team/roster` deliberately serves id/name/role/color and **not** email addresses, since `SITE_LOGIN_ENABLED` is off and anything that endpoint returns is effectively public. Addresses are read server-side only at send time, each overridable via `TEAM_EMAIL_<ID>` without a deploy; Marco has no hardcoded address — as the OAuth sender his own inbox resolves from the Gmail profile. Verified with 13 browser checks against a clean notification store (gate appears/closes/persists/reopens, badge count exact, badge survives reload, switching users repoints both).

- [2026-07-28] — **Notifications now pop immediately on login, not just on the bell badge** (`public/team-tasks.html`, `public/shell.html`). Both pollers (Task Command's own and the shell-level one added the day before) had a `firstNotePoll` guard that suppressed every popup on a page's very first notification poll — meant to avoid flooding a fresh page load with historical chatter, but its actual effect was that an assignment made while you were logged out never visibly notified you at all: `updateBell()` still ran (so the bell badge was correct), but the toast that's supposed to say "you have a new task" only ever fired for notifications that arrived *while the tab was already open*. Fixed by dropping the first-poll guard and popping on any poll — including the first — whenever a notification is genuinely unread (`!n.readAt`, the real server-side signal) and hasn't already been shown to this browser (`tt_seen` / `shell_note_seen`, unchanged). Net effect: log into Task Command (or open the shell on any tab) with a pending assignment waiting, and it now surfaces as an actual popup at that moment instead of a badge you have to notice. Also confirmed while investigating this that "assign to any team member" already existed and was never restricted — `team-tasks.html`'s `TEAM` array (marco/wesley/kendrick/carlos) populates the New Task modal's Assign To dropdown unconditionally regardless of who's logged in; no code change was needed there. Verified live: an assignment made to an offline user, followed by that user loading `/team-tasks` (or `/shell` on the CRM tab) fresh, now pops the notification within ~2s of the page's first poll, in both surfaces.

- [2026-07-27] — **Shell-level task-assignment popups** (`public/shell.html`). Task Command (`public/team-tasks.html`) already creates a notification and pops a toast the moment someone assigns you a task via `/api/tasks`, but that popup only rendered inside the Tasks iframe — since inactive tabs in the shell are `opacity:0;pointer-events:none` rather than `display:none`, its poller kept running invisibly whenever Tasks wasn't the active tab, so an assignment made while you were in the CRM (or any other tab) produced no visible signal. Fixed by mirroring the same polling logic one level up, in `shell.html` itself (`GET /api/team/notifications?user=<id>` every 20s, identity read from the same `marcoTaskUser` localStorage key the Tasks "who's on?" gate writes — no separate sign-in needed): a new stacking `#notePops` popup list renders on top of whichever tab is active, dedupes via its own `shell_note_seen` localStorage key (mirroring `team-tasks.html`'s `tt_seen` pattern, including first-poll suppression so existing unread notifications don't flood on page load), and is explicitly suppressed only when the Tasks tab itself is active (since that page already shows its own copy — this is what stops a literal double popup in that one case). Clicking a shell-level popup jumps to the Tasks tab via `window.AppShell.open("tasks")`. Verified live with two-browser-context Playwright: an assignment while the assignee sits on the CRM tab now produces a visible shell popup with correct sender/task text that navigates to Tasks on click; the same assignment while already on the Tasks tab produces exactly one popup (the in-iframe one), not two.

- [2026-07-27] — **Tracker staging: real-signal auto-stage + bulk manual stage** (`src/core/trackerEngagement.ts`, `POST /api/tracker/stage-from-engagement`, `POST /api/tracker/records/bulk-stage`, select mode in `public/tracker.html`). Every one of the 1,219 tracker records was Unstaged with no automatic signal available — the CRM's `crmStage` and Brivity's own stage field are both "new" for nearly everyone, and per-lead message *text* was ruled out as a source (silent judgment calls on free text is exactly what this codebase's migration conventions forbid). The one real, structured signal that exists: `Lead.state`, the DM funnel's own step tracker. `criteria_collected` / `property_sent` / `closed` only ever get set after a genuine two-way exchange (phone/criteria given, a property breakdown sent) — see `funnelDeterministic.ts`. **Important**: DM `state: "closed"` means the *conversation* concluded, never a sold/bought property; it must never be read as the tracker's own `closed` stage, and this code doesn't. `stageTrackerFromEngagement()` moves exactly one step, Unstaged → Contacted, only for the side(s) a record has, and only when that side has no stage yet (never overwrites manual work) — nothing past Contacted is ever inferred, since qualifying, showing, and offers need a real call this data can't see. Dry-run by default, same convention as the CRM backfill. On the real tracker this staged 228 buyers (0 sellers — no seller ever reached that DM depth) and is a verified no-op on re-run. For the ~991 records with no DM signal at all, the tracker's new **Select** mode (checkboxes, "Select all shown", bulk move-to-stage) is the lever — that's real human/Harvey-driven work, not something this endpoint claims to know. Verified with 20+ browser checks against a fresh-seeded mirror of the real 1,219 records, including the confirm-dialog copy, idempotency, and that switching Buyers/Sellers clears the selection (stage keys differ between the two pipelines).

- [2026-07-27] — **Harvey Jobs panel** (`public/jobs.html`, route `/jobs`, new "Harvey Jobs" tab in the shell above Tracker). Jobs were API-only; this is the operator surface. Left pane starts a job (free-text instructions, ⌘/Ctrl+Enter), right pane shows the selected job's prompt, result, every step with tool name and truncated input/output, and any failure reason. Files written during a job are lifted out of the `write_file`/`edit_file` steps into a **Files written** list that reads the real content from `/api/harvey/workspace` on click, so a job's output is one click away rather than something to go hunting for. Live jobs show a spinner and a Cancel button and the page polls every 2.5s **only while something is running**, so an idle tab is silent. Interrupted jobs state plainly that the server restarted. Verified with 22 browser checks across all four job states.

- [2026-07-27] — **Harvey can now produce files and run work to completion** (`src/core/workspace.ts`, `src/core/jobStore.ts` → `jobs.db`, `src/hull/jobRunner.ts`, 8 more tools). Two gaps closed. (1) **Workspace**: a sandboxed directory under `/data/workspace` (`HARVEY_WORKSPACE_PATH` to override) with `list_files`/`read_file`/`write_file`/`edit_file`/`delete_file`. Every path is resolved and proven inside the root before any I/O — traversal, absolute paths and symlinks are **refused, never sanitised** — with a 2 MB cap and a document-only extension allow-list (no .js/.sh/.py). `edit_file` refuses an ambiguous match rather than rewriting the wrong line. (2) **Jobs**: `startJob()` runs the agent loop detached from the HTTP request with a 40-step ceiling instead of the interactive 8, writing every step to SQLite as it goes so progress is readable while it runs. Jobs left `running`/`queued` by a restart are marked `interrupted` at boot — nothing resumes them, and a phantom running job is worse than an honest failure. Failed jobs are **not** auto-retried because these tools send email and move real records. API: `POST /api/harvey/jobs`, `GET /api/harvey/jobs[/:id]`, `POST /api/harvey/jobs/:id/cancel`, `GET /api/harvey/workspace`. Harvey's loop now offers 74 tools. Verified with 38 checks: 8 traversal attacks refused with nothing written outside the root, full job lifecycle, mid-run progress visibility, cancellation, and restart reconciliation.

- [2026-07-27] — **Harvey can now see the platform surfaces that were siloed** (`src/harvey/platformTools.ts`, 12 tools, spliced into `HARVEY_TOOL_DEFINITIONS` and routed ahead of the main switch in `harvey/tools.ts`). Harvey had ~54 tools covering leads, ads, finance, content and transactions, but **none** for the Buyers & Sellers Tracker, Task Command, the team board or command settings — it could not see the 1,219 tracker records or the 651-task board the team actually works from. Added: `get_tracker_summary`, `get_tracker_pipelines`, `search_tracker`, `get_tracker_record`, `set_tracker_stage`, `update_tracker_record`, `push_tracker_checklist_to_tasks`, `get_task_board`, `create_task`, `update_task`, `get_team_status`, `get_command_settings`. Writes are deliberately narrow (stage moves, status/notes, task create/update, checklist push) and nothing deletes. Invalid stages and statuses are rejected with the valid list returned rather than silently coerced. The agent loop now offers 66 tools; verified with 30 checks against 400 real tracker records including every write path.

- [2026-07-27] — **Brivity contacts imported into the lead store** (`src/core/brivityImport.ts`, `POST /api/brivity/import/plan` and `/apply`). `brivityPeople.ts` could already read Marco's real Brivity book (2,595 contacts) for `/crm`, but nothing had ever been persisted, so no lead carried a `brivityId`. The planner matches by `brivityId` → normalised 10-digit phone → email and reports field-level changes before writing; apply re-plans server-side first. Result: **517 leads created, 189 merged, 0 failures** — lead store 746 → 1,263, tracker 702 → **1,219 (865 buyers, 354 sellers)**. Dead contacts (1,802) are held back from creating rows but still *enrich* leads we already hold, which recovered 159 real names for records sitting on the board as social handles ("purple kitty 22" → Andrea Perez); a Brivity "dead" status is never copied onto an existing lead. **`db.upsertLeadQuiet()` is the import write path and must stay that way** — `createLead()` fires real outbound automations (Twilio SMS to Marco and Carlos on a phone; a marketing auto-reply plus buyer/seller drip on an email), so importing through it would have sent ~1,000 texts and emailed 370 cold contacts. Verified by running both paths in one process on identical data: createLead produced 6 outbound automation lines, upsertLeadQuiet produced 0, and production logs during the real import show zero. Also: `backfillTrackerFromLeads` gained `refreshIdentity` (default off) so the tracker could pick up the renamed leads, and the Brivity fetch timeout went 30s → 90s with one retry because the full pull takes 27-30s and was failing intermittently.

- [2026-07-27] — Task Command time zone picker now offers every zone the browser knows (~419 via `Intl.supportedValuesOf`), grouped by region with the team's usual zones pinned first and each option showing local time plus GMT offset. The server already accepted any valid IANA zone, so the hardcoded list of 14 was the only limit.

- [2026-07-27] — **Tracker checklist wired to the Task Manager** (`src/core/trackerTasks.ts`). A checklist item can be pushed onto the Task Command board as a real `CommandTask`; the item then stores its `taskId` (new optional field on `CommandTaskChecklistItem`) and that one id is the whole link — there is no second copy of the state to drift. Completion travels both ways: ticking in the tracker PATCHes the task to `done`, and the tracker's read path (`applyTaskState`) overlays live task status onto linked items, so completing it on the board shows up without a tracker write. Generated tasks carry `color: purple` and tags `tracker` + `tracker:<recordId>`, are assigned to the record's assignee when it maps to a known team id (else `carlos`), and get a description naming the person and their phone. Deliberately **not** synced: item text — renaming a checklist line must not silently rewrite a task someone is working from. Deleting the task clears the link and leaves the item; unlinking leaves the task. New endpoints: `POST /api/tracker/records/:id/tasks` (push, `itemIds` optional, idempotent per item), `DELETE /api/tracker/records/:id/tasks/:itemId` (unlink). `GET /api/tracker/records/:id` and the PATCH response now also return `tasks` (linked task status/column/assignee) so the drawer's chips are never stale. Verified with 32 API checks plus a browser pass that confirms the task really lands on `/team-tasks`.

- [2026-07-27] — **Tracker backfill applied to production** (v393). 746 leads scanned → 741 created, 5 skipped; re-runs are a verified no-op (`created 0, updated 0, skipped 746`). No record needed stage review because every lead was `crmStage: "new"`, so all of them landed Unstaged with no buyer/seller stage. Two data problems the live run exposed, both now fixed in code: 44 records imported named literally `{{full_name}}` (unresolved ManyChat merge fields — the skip rule only fired when a lead had no name *and* no contact details, and the placeholder counted as a name), and the source filter listed `Instagram` and `instagram` as separate options matching 428 vs 1 records. `cleanName()` in `trackerMigration.ts` now strips `{{…}}` before the skip check; the source filter groups case-insensitively with canonical labels. Cleanup on the live data: the 39 placeholder rows with no phone or email were deleted, the 5 that did have a phone were renamed to it rather than dropped. Live total is **702**. Also fixed a card-squashing bug that only appears at volume — `.colbody` is a flex column, so a full column shrank every card and clipped its text instead of scrolling (`flex:0 0 auto` on `.rec`); the board renders 741 records in ~76ms.

- [2026-07-27] — **Tracker UI** (`public/tracker.html`, route `/tracker`, new `Tracker` tab in `shell.html` between CRM and DM Agent). A kanban board over `/api/tracker/*`: one column per stage for whichever side is selected, plus a leading **Unstaged** column so imported records are visible rather than hidden. Cards carry a left side-banner — buyer indigo `#4F46E5`, seller orange `#EA580C`, split gradient for two-sided — deliberately outside the task-status palette (slate/blue/amber/green/red) so a tracker card can never be misread as a task state. Drag-and-drop between columns moves the stage and saves immediately; entering the timeline stage (buyer *Qualified* / seller *CMA Requested*) prompts once for the timeline date and stores it in `stageMeta`, and cancelling that prompt leaves the card where it was. A right drawer edits contact details, status, notes and the checklist (Save button), and holds one vertical stage rail **per side** — a two-sided person shows two rails that move independently, and their other-side stage is also shown on the card while working the opposite board. Filters (status chips as a union, source, assignee, added-date range, and a search covering name/phone-digits/email/address/notes) run client-side. Contact age is only reported when `lastInteractionAt` is actually set — records with none read "No contact logged" rather than inheriting `updatedAt` and claiming "Contacted today"; past 14 days it turns red. Verified with a 65-check headless pass (`ALL CHECKS PASSED`) covering both pipelines, drag persistence, filters, drawer save, delete, dark theme and reload.

- [2026-07-27] — **Buyers & Sellers Tracker data model** (backend only, no UI yet). New store `src/core/trackerStore.ts` → `tracker.db` (`TRACKER_DB_PATH` to override), table `tracker_records`. Replaces the CRM's single shared stage list with two real pipelines: 11 buyer stages (Contacted → … → Past Client) and 15 seller stages (New → … → Past Client), plus a 6-value status (`new/unqualified/watch/nurture/hot/pending`) that is orthogonal to stage. A record carries `sides: ("buyer"|"seller")[]` and **holds `buyerStage` and `sellerStage` independently**, so a lead selling one house and buying another advances on both tracks at once. Per-stage metadata (`stageMeta`, keyed `"buyer:qualified"`) stores the entered-at timestamp plus the timeline date the spec asks for at Qualified. Records also carry a `checklist` (same shape as `CommandTask.checklist`) and `taskIds`, which is the hook for the Task Manager sync. API: `GET /api/tracker/schema|records|counts`, `GET/PATCH/DELETE /api/tracker/records/:id`, `POST /api/tracker/records`, `POST /api/tracker/records/:id/stage`, `POST /api/tracker/backfill`. Backfill (`src/core/trackerMigration.ts`) is **dry-run by default** (`{"apply":true}` commits), idempotent, and non-destructive — it only gap-fills existing tracker rows, keeps each lead's original stage in `legacyStage` so a bad mapping can be re-derived in bulk, and returns a `needsReview` list for the three legacy stages that have no clean two-pipeline equivalent (`appointment_set`, `showing_set`, `pending`). Deployed live in v391; the dry run has been executed against production but **nothing has been applied** — `tracker_records` is still empty. That dry run surfaced the finding below: all 746 production leads sit at `crmStage: "new"`, so there is no pipeline history to migrate and the ambiguous-stage mappings never fire.

- [2026-07-26] — Task Command gains a team-wide **command time zone** (`src/core/commandSettings.ts`, `GET/PUT /api/settings/command`, JSON store at `command-settings.json`, default `America/Chicago`). Day boundaries, deadline labels, "due today" and rollover now resolve against that one zone instead of the viewer's machine, which is what made Marco and Wesley disagree about which day a task was due. Also: incomplete tasks carry forward to today pinned above the rest with a red Missed Task banner (display-time, `dueDate` is never rewritten, so the original date and days-late stay accurate); and the dashboard split is user-resizable via a draggable divider with a swap-sides toggle, persisted per user in localStorage.

- [2026-07-24] — Task Command: `CommandTask` gains an optional `checklist` (`CommandTaskChecklistItem[]`, parsed and capped server-side on POST/PATCH `/api/tasks`), surfaced as a checkbox list under Details / Notes with a progress chip on the card. Task Description became an auto-growing textarea (newlines collapsed on save so the title stays one logical line). Added `color-scheme` per theme plus solid `select`/`option` colors, which fixes native dropdowns and date pickers rendering washed out in dark mode.
- [2026-07-24] — Ad Manager tab removed from the app shell at the owner's request. Nothing deleted: `public/ads.html`, `src/core/adsStore.ts`, the `/ads` route and `/api/ads/*` all still work, so restoring it means uncommenting one `TABS` entry in `public/shell.html`.
- [2026-07-24] — People smart filters now actually filter. They were a dead `<a>` with no list and no handler; `mapPeopleLive` was also dropping the fields predicates need (`statusDisp`, `alerts`, `reports`, `autoPlanEnrollments`), so reusing the Leads predicates would have silently matched every row. Filters needing website-visit data are shown but flagged as having no source data instead of returning everything, and the same three no-op `=> true` filters were fixed in the Leads list.
- [2026-07-24] — DM agent: re-anchor reply when a lead is confused by the first-time-buying opener, and a smoother recovery when a lead says they already sent a number that never landed. Known gap: numbers sent inside a quoted/reply bubble still are not captured, since only `message.text` is read.
- [2026-07-24] — Initial FORAI.md added; repo joined the Chronicler's nightly sweep.
- [2026-07-24] — CRM Dashboard: "Filtered" chip now opens a real multi-select Timeline Filter popup, Take Action refresh button wired; Calendar events are derived from transaction records and click through to the matching transaction row.
- [2026-07-24] — Ad Manager tab added (`public/ads.html`, `src/core/adsStore.ts`, `/api/ads/*`). Live Meta *reporting* reuses the existing `AD_DASHBOARD_BASE_URL` proxy; campaign creation is a local planner only — there is no write-capable Meta Marketing API in this codebase.
- [2026-07-24] — **Security:** site-wide login, team accounts, and an action audit trail added (`src/core/authStore.ts`, `public/login.html`, `public/team.html`, `/api/auth/*`). Deliberately left **unenforced** behind `SITE_LOGIN_ENABLED` at the owner's request — the app is currently open. Uses Node's built-in `crypto.scrypt` and hand-rolled cookie parsing so the no-npm-install overlay deploy path keeps working.
- [2026-07-23] — CRM Email Marketing wired to real Gmail sending via the existing OAuth link; Newsletters list and per-row delete added.
- [2026-07-23] — Transactions CSV import in Brivity export format; system-wide always-visible top scrollbar synced to each table's bottom bar.

## Known gaps / open issues

- **Gmail's refresh token is dead — nothing outbound is sending.** Confirmed live 2026-07-28: `/api/email/connection-status` returns `invalid_grant: Bad Request`. `invalid_grant` (not `invalid_client`) is the key detail — **the Fly secrets are all present and correct** (`GMAIL_CLIENT_ID`, `GMAIL_CLIENT_SECRET`, `GMAIL_REFRESH_TOKEN` all show Deployed; bad app credentials would report `invalid_client`). Google has invalidated the refresh token itself. This kills **every** outbound path — drip campaigns, digests, and task-assignment emails. It is not caused by any recent code change. This is the second occurrence (the previous token died 2026-06-22), and a *recurring* `invalid_grant` on an otherwise untouched token is the signature of an OAuth consent screen still in **"Testing"** publishing status, where Google expires refresh tokens after 7 days. **Relinking restores sending but will die again in a week unless the consent screen is published to "In production"** in Google Cloud Console. Relink (no deploy, no secret rotation): open `https://marco-90-automation.fly.dev/api/email/gmail-oauth/start` signed in as Marco; the fresh token is written to the email DB on `/data`, which `getConfig()` prefers over the env var — note this makes `tokenSource` flip `env` → `db`, so the stale `GMAIL_REFRESH_TOKEN` secret stops being consulted.
- **The CRM stage field has never actually been used.** All 746 production leads are `crmStage: "new"` (confirmed 2026-07-27 via the tracker backfill dry run). Every stage-derived view — funnel counts, stage filters, the tracker migration — therefore has no real history to work from, and the tracker's pipelines will be populated by hand or by automation going forward, not by migration.
- **The CRM shows 230 of 746 leads.** `getDashboardSnapshot()` does `if (!hasPhone) continue`, so the ~511 Instagram/TikTok DM contacts who never gave a phone number are in the lead store but invisible in the CRM UI (only 4 leads have an email). This is deliberate, but it means "how many leads do we have" has two different right answers, and the tracker backfill currently targets the 746, not the 230.
- **The tracker holds 1,219 records and every one of them is Unstaged.** Neither the CRM nor Brivity has ever used its stage field (2,591 of 2,595 Brivity contacts are stage "new"), so there was no pipeline history to import and stages must be set by hand or by future automation. 747 of the 1,219 are contactable (742 phone, 448 email); the other 472 are Instagram/TikTok handles with no phone or email, hidden behind the tracker's "Contactable only" filter.
- **Nothing syncs the CRM and the tracker after the import.** `leadId` is set on backfilled records, but no code updates a tracker record when its lead changes (or the reverse), so the two drift from here.
- **Tracker→task links are per-item and manual.** Pushing a checklist item to the Task Manager is an explicit action; nothing auto-creates tasks from a stage change or a new checklist item. Task→tracker completion is resolved at read time, so it is correct whenever the tracker is loaded but there is no push notification to an already-open tracker tab (hit Refresh).

- **Security — the app is currently unauthenticated.** `SITE_LOGIN_ENABLED` is off in production (owner's explicit decision: "keep it in the back end for now"), and `dashboardTokenOkIncoming()` returns `true` whenever `DASHBOARD_TOKEN` is unset. Re-enabling is a one-secret change, no code edits.
- **Default branch is wrong.** GitHub reports `master` as the default branch, but `master` is a stale, *unrelated* history (7 commits, last real change 2026-07-20) with no merge base against `main`. All 267+ commits of real work, and every deploy, live on `main`. This file is on `main`. Until the GitHub default is flipped to `main`, the Chronicler's nightly sweep will look at the wrong branch and miss this file.
- **`server.ts` is a 10.5k-line monolith** with ~395 routes on a single `app` and no Routers. It works, but every feature widens the same file; it is the main structural debt here.
- **Ad Manager is hidden and is reporting + local planner only.** Its shell tab is commented out (owner's request), though `/ads` and `/api/ads/*` still serve. There is no Meta Business/ad-account write access, so nothing in the app can create or edit a real campaign. Doing so needs a System User token, an ad account ID, and spend guardrails (budget caps, approval-before-live) that do not exist yet.
- **No website-visit tracking.** Several Brivity smart filters ("site visit today / this week / last 30 days") depend on it, so in both Leads and People they are listed but disabled with an explanatory toast rather than silently matching every row.
- **`AD_DASHBOARD_BASE_URL`** points at `https://marco-agents.fly.dev`, which is not currently reachable; the Ad Manager degrades to an honest "unreachable" badge.
- **Content pipeline is TikTok-only in practice.** `src/core/socialStore.ts` hardcodes `const PLATFORM = "tiktok"`, so scoring/analytics are not Instagram-native despite the UI implying multi-platform.
- **No test suite and no linter.** `npm run build` (tsc) is the only automated gate; verification is manual, usually a headless-browser pass before deploying.
- **Several integrations are stubs** that throw or no-op: `integrations/manychat` (`sendDM`), `integrations/amazon`, `integrations/newhomebuddy`, `integrations/brivity/index.ts` (the real client is `integrations/brivity.ts`).
- **`voxcpm-service` cannot run in production** — it needs CUDA and ~8 GB VRAM, so voice-clone TTS relies on ElevenLabs instead.
- Deploys cannot add npm dependencies via the fast overlay path; a new dependency requires a full image rebuild.
